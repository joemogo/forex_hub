# POST-MOGO-021 HARDENING BACKLOG

Deferred P2/P3 items. **None of these blocks CORE GREEN or safe PAPER operation.**
Each was found by independent adversarial verification during MOGO-021, classified under the
risk-based convergence policy, and deliberately deferred rather than fixed.

Kept separate from MOGO-021 readiness on purpose: these can be worked incrementally while MOGO
accumulates forward PAPER evidence.

---

## P2 — Operational / observability

| # | Item | Why deferred |
|---|---|---|
| B-1 | **MOGO-013 observation ledger has no behavioural coverage.** `evidencePutObservation` and `evidenceEnforceObservationRetention` are never called on any path the gate observes; eight behaviour-changing mutations survive, including eviction that deletes nothing while the RETENTION marker records "evicted, NOT recoverable". | Research corpus, not trade evidence. Cannot alter a trading decision, a paper position, or the trade-evidence packages (which are covered by PTE2E-EVCAP.*). |
| B-2 | **`setTimeout` never fires in the JS harness.** 14 production sites are unexercised, including the boot sequence and four `await new Promise(res=>setTimeout(res,0))` yields that would hang the harness if reached. A timer-deferred cross-strategy leak survives ISO. | Isolation is verified for synchronous and microtask-deferred writes at any depth. No production route defers a cross-strategy write by timer. Making `setTimeout` real risks hanging four suites — a change that needs its own verification budget. |
| B-3 | **26 JPY `toFixed(5)` render sites**, including the manual-review confirm modal and an editable R:R input. `alexPx()` already exists. | Values are correct; only the precision display is wrong (`150.12300` vs `150.123`). Cannot change a trading decision or a booked number. |
| B-4 | **JVM `WRITE_FAILED` is not classified.** `savePaperAccountGuarded`'s JVM branch omits `evidenceClassifyStorageError`/`evidenceRecordWriteFailure`; the ALEX twin has them. On a quota-exhausted commit the operator is told "a newer version may exist in another tab", which is a different remedy. Also `ROLLBACK_FAILED` lacks them on both arms, and the ALEX reporting that does exist is itself unpinned. | The commit is still refused fail-closed, so no durable corruption. The operator is misinformed about the cause, not about the outcome. |
| B-5 | **`commitPaperLedger` flattens the refusal reason.** `LOAD_INTEGRITY_BLOCKED` and `blockedKeys` from `savePaperAccountGuarded` are replaced by the generic blocking-banner text, so a caller cannot distinguish an INC-001 refusal from an ordinary stale-version rejection. | Diagnostic detail. The refusal itself is correct and now pinned (PTE2E-INC001.1). |
| B-6 | **`evidenceHasPackageForTrade(null)` returns a match**, so a trade with no id would be treated as already captured. | No production path produces a null tradeId; the id is minted before capture. |
| B-7 | **Backfill and auto-export are ALEX-only.** `evidenceBackfillFromLocalStorage` reads only the ALEX stores, so a JVM-only operator gets "Examined: 0" with no explanation; `evidenceExportPending('AUTO_DOWNLOAD')` has one call site, inside the ALEX seam. | Feature asymmetry, not a defect. JVM trades are captured live by their own seam. |
| B-8 | **`runHistoricalDataDiagnostic` has no termination-reason field.** `requestCount:20` reads identically for "needed 20 pages" and "hit the page ceiling mid-history", and the UI labels the truncated boundary "Actual earliest candle". | Diagnostic reporting. Does not feed trading logic. |
| B-9 | **`alexGAutoTrading.log` has no reader.** The ALEX per-setup rejection audit trail exists only in localStorage; no UI renders it. | Missing surface, not wrong information. Forward PAPER operation will make the need concrete. |
| B-10 | **`alexV2AutoTrading` snapshot asymmetry.** Present in `snapshotJvmStores`, absent from `snapshotAlexStores` and from the reset, so an in-memory ALEX→alexV2 mutation without a save passes both. | alexV2 is research/shadow and gates nothing. A persisted leak is still caught by the `fxhub_alex*` key comparison. |
| B-11 | **`currentChartCandles` is not cleared** on `loadChart`'s empty-fetch early return, so the module briefly holds the previous pair's candles under the new pair's identity. | Latent: `destroyChart()` nulls `lwChart`/`candleSeries` and all six consumers are gated on them. Protection is incidental, so worth making explicit — but not reachable today. |
| B-12 | **A JVM package carries `strategyVersion:"alex_g_sr_v1 (inferred, unstamped)"`.** | Explicitly labelled inferred and unstamped; `identity.strategyId` is correct and pinned (PTE2E-EVCAP.8). Odd, not wrong. |

## P3 — Test / maintenance

| # | Item |
|---|---|
| B-13 | ~264 source-text assertions in `tests/v128_evidence_platform_tests.js` (`String(fn).indexOf(...)`). Non-behavioural and inverted-risk: a behaviour-preserving refactor breaks them, a behaviour-changing edit that keeps the phrase passes. Several evidence-layer claims rest only on these. |
| B-14 | The in-memory IndexedDB stub still does not model: transaction isolation (writes visible before commit), `TransactionInactiveError`, key coercion/`DataError`, cursor `delete()`, `count()`/`openCursor()` ranges, `versionchange` blocking, or a failing `open()`. |
| B-15 | No end-to-end fixture composes *real close → canonicalise → SHA-256 → store → verify*: the e2e runner has no `crypto.subtle`, so packages are stored with `contentHash:null`. v128/v131 supply crypto but drive the builder with synthetic trades. |
| B-16 | The ALEX tradeId duplicate guard's `closedPositions` half is unwatched. Production is correct and the disclosure that it was "redundant" was refuted — reaching it in a fixture needs `fxhub_alexg_setups` to load-fail while `closedPositions` survives. |
| B-17 | `exitDetectedAt` unpinned; `evidenceCaptureClosedPaperTrades`' 25-record window boundary unpinned at N-1. |
| B-18 | **RESOLVED — the three surviving mutations are now killed, all behaviourally, and no production line changed.** `calcBiasFromCandles` had its OUTPUT asserted (a label swap and a bias suppression both kill) and its production consumer pinned by AUTOADMIT.1-3, but three INTERNAL rules were undiscriminated and all three survived the MOGO-021 audit: lowering the 55-bar history floor to 25, and dropping EITHER EMA20-vs-EMA50 leg from the trend test. **These were missing tests, not a defect.** The function is frozen strategy semantics, its documented contract is unambiguous, and nothing in the evidence contradicts its behaviour — so the repair is fixtures only. No threshold, weight or rule was altered, and `index.html` is byte-identical (sha256 re-verified after every mutation run). **8 fixtures added as S12 BIASTHRESH-1..8 in `tests/v1237_detection_controls_tests.js`**, the suite built for exactly this class of gap, under its standing rule — THE CANDLES ARE CONSTRUCTED, THE VERDICT IS NOT. `calcBiasFromCandles` and `calcEMA` are exposed on the runner's `g` and are CALLED, never re-implemented; `calcEMA` is exposed so each fixture can re-prove from the engine's OWN EMA which leg it is contesting rather than assuming it. **Every negative sits ONE VARIABLE from a proven positive:** 54 candles → `—` against 55 candles → `Bullish` (the floor is `<55`, not `<=55`); a 40-candle rising series refused, which a 25-bar floor would read as Bullish; a bounce where `close>EMA20` but `EMA20<EMA50` with a higher high → `Ranging`, paired with a clean uptrend where both legs hold → `Bullish`; and the mirrored pullback → `Ranging` paired with a clean downtrend → `Bearish`. Each EMA fixture asserts its preconditions (`close>EMA20`, `!(EMA20>EMA50)`, higher high present) BEFORE asserting the verdict, so it cannot silently degrade into a series where the leg was never contested. Plus null/`[]` refusal. **3/3 MUTATIONS KILLED, ALL BEHAVIOURAL, each by exactly ONE assertion** — floor 55→25 killed by BIASTHRESH-1, the trendUp leg by BIASTHRESH-4, the trendDown leg by BIASTHRESH-6 — plus a no-op control that correctly survived. Applied to COPIES of `index.html` in temporary trees, one anchor match each, `node --check` validated, collection pinned at 96, named discriminator required. The first run named BIASTHRESH-3 as the floor discriminator and the kill landed on BIASTHRESH-1 instead; the expectation was corrected rather than the fixture, since the 54-vs-55 boundary is the tighter discriminator. `expected_fixture_counts.tsv` updated for the one suite (88 → 96); JS lane 36 suites / **2762** assertions / 0 failures / 0 execution errors. **NOT strategy tuning:** no bias threshold was changed, and nothing here was informed by the preserved losing ALEX subset. |
| B-19 | `v1211_diagnostics_integrity_tests.js` counts `placePaperTrade` among "real production functions exercised", which overstates what it asserts (it uses the function as a vehicle to dirty state). Now separately covered by PTE2E-PLACE.0-5. |
| B-20 | `findAOIsWide` window and `hasAOIOverlap` tolerance are unpinned, but reachable only from `simulateBacktest` — research/backtest, never the live trade path. Classified EQUIVALENT/NON-CRITICAL by the final sweep. |
| B-21 | **RESOLVED (v12.39.3).** Implemented as an observability-only mirror, which is what the operator approved. `fxhub_alexg_exit_monitor` is written by `alexGCheckLivePositions` and never read back by the engine. The backlog previously framed "persist the cursor" as the remaining item; that framing was wrong. The in-memory cursor SELF-HEALS — on reload it reverts to `openedAt` and re-walks a superset, which is why no exit can be missed and why MAE/MFE on a closed trade are full-life correct. Persisting it would make an over-advance permanent across sessions, trading a real safety property for a reporting one. What was missing was a REPORT, not a RECORD. 4 fixtures (XMIRROR-1..4), mutations 3/3 killed. The load-bearing fixture had to be rewritten: asserting that a mirror cursor a day ahead does not move the ACCOUNT cursor PASSED against an engine that reads the mirror back, because a future range returns no candles and the cursor only advances over candles walked — the harm is that the engine SKIPS candles, so XMIRROR-3 now observes the fetch range. No protected function changed. |
| B-22 | **PARTIALLY EXECUTED, in the authorized sequence.** Steps 1-2 COMPLETE: `scripts/preserve_paper_ledger.js` preserved the PAPER ledger from a read-only copy of localStorage (it refuses a live directory), and the capture was independently verified by a different route than the original investigation — ledger-side joined to package-side, versus observation-side. Confirmed: 39 closed / 35 real / 4 developer TEST; 25 within the automatic re-mint window; 10 REAL outside it; the 9 missing packages form a contiguous oldest block with the boundary between 2026-07-17T09:00:28.782Z and 11:28:00.220Z. Ledger rollup pinned in `docs/trader-intelligence/evidence/ledger-preservation/`. Step 5 COMPLETE AHEAD OF THE BACKFILL, which is the safe order: `HISTORICAL_BACKFILL` maps to `journal_entry`, deriving a distinct **RECONSTRUCTED** population — never `paper_trade` (which would pool a MINIMAL/UNSAFE_TO_RECONSTRUCT record with live-captured ones) and never UNKNOWN (which must keep meaning "cannot determine"). Mutation-verified, including the importer-level mapping test whose absence let a one-character contamination edit survive. **Step 3 — clicking "Backfill history (read-only)" — is NOT done**: it requires driving the operator’s live tab, and P0 requires that B-22 not jeopardize a healthy forward runtime. One operator click is all that remains, and the before/after machinery to verify it immediately afterwards is in place. |

| B-23 | **RESOLVED.** Five Python suites asserted a corpus snapshot instead of an invariant (`nodes == 43`, `edges == 79`, `questions == 12`, `phase7a_nodes == []`, evidence tree empty). Three of them asserted a tree that MOGO-022's own authorised import deliberately filled. All five were rewritten as relations and mutation-verified; the Python suites were also wired into `tests/run_all.sh`, which had never run them at all — which is why the failures went unnoticed. An independent verifier defeated the first rewrite in five ways, all since fixed and re-verified: the contentHash assertions computed the expected value with the same `content_hash_of` the builder used (gutting it to drop every list/dict field left everything green — the tests now compute the hash independently); the read-only test fingerprinted 5 of 19 evidence subdirectories and was defeated by a sibling test that ran the build first (now all subdirectories, comparing mtime as well as content); `len(edges) > 0` was satisfied by 1 edge instead of 5142 (now an identity-pinned edge-type set plus an independently derived `BELONGS_TO_TRADER` count); the per-category non-emptiness guards let a whole node type vanish (now per node type); and the blocking-question filter shared its expression with the implementation (now a stub-index positive control forcing `answerStatus` and `researchStatus` to disagree). |
| B-24 | **RESOLVED.** Three coverage gaps left open by B-23, each closed and mutation-verified. (a) Discovery fabrication was caught only for the 9 node types that re-read their entity from disk; for the other node types a wrong-but-existing `sourceFile` was invisible, because the graph test compared against the same generator the build used. The cited file must now CONTAIN the entity's own id — a substring check over the raw text, which handles one-entity-per-file and the array-in-one-file cross-reference uniformly. Rotating every CLAIM's `sourceFile` onto a different real claim file is now caught. (b) Edge completeness was independently derived for `BELONGS_TO_TRADER` only, so a derivation dropping most of its edges would pass. Counts for `CLAIM_SUPPORTS_HYPOTHESIS`, `BLUEPRINT_DERIVED_FROM_CLAIM` and `RAISES_QUESTION` are now derived from the entities' own reference fields — 2880 of 5142 edges under an exact count, the rest under the identity-pinned edge-type set. Dropping half of one type, or all but one, is now caught with the exact numbers. (c) Closing the `DUPLICATE_NODE_ID` control found the guard is emitted TWICE — at construction and at validation — and the existing fixture test's OR over two categories rested entirely on the construction one; the validator's guard, and `DUPLICATE_EDGE_ID`, could both be deleted with nothing failing. Both now have direct controls. |
| B-25 | **Forward RECOVERABILITY is capped at 25 trades, and the cap tightens with every close.** This is split out of B-22 because B-22 states "historical only; no forward capture gap", and while the *capture* claim remains correct — no live close has ever been missed — the *recoverability* claim is not. `evidenceCaptureClosedTrades` snapshots `alexGAccount.closedPositions.slice(0,25)` (`index.html:15951`), and `closedPositions` is newest-first. Packages are never evicted (`evidenceEnforceBufferLimits`, `:16327-16340`, evicts setups and zones only), so store destruction is the sole package-loss vector — but if the evidence IndexedDB is lost again, the automatic seam re-mints only the newest 25 of the account's closes. At 35 real closed ALEX trades that is a further 10 silently absent, and the shortfall widens by one per close. The JVM twin has the identical window at `:16293`. The only remedy is the operator clicking "Backfill history (read-only)" (`:16908`), which iterates all of `closedPositions` with no window — but nothing surfaces the shortfall and nothing tells the operator to do it. **This is also the mechanism behind B-22**: the 2026-08-17 store wipe was the trigger, but the 25-record window is what determined WHICH trades were lost and why the loss is a clean contiguous oldest block. B-17 (the same window's N-1 boundary, unpinned) and B-22 are therefore one defect seen from three angles. Neither capture function is among the 63 protected functions, so changing the window does not raise the governance bar to protected-semantics — but it changes how much work the capture seam does per tick and is unpinned at the boundary, so it needs owner sign-off plus an N-1 fixture before any change. |
| B-26 | **RESOLVED as a consequence of B-27.** Orphan EvidenceSource records accumulated one per import run per stale capture file, because every run minted a fresh id for a file whose packages were all already imported. With identity keyed on the artifact, a repeat run reuses the recorded id and mints nothing — verified against the live corpus. The two orphans created before the fix (`EVSRC\|MOGO\|20260818\|016`, `EVSRC\|MOGO\|20260819\|015`) are left in place: no observation cites them, the evidence validator reports 0 INFO/WARNING/ERROR/FATAL, and deleting a recorded provenance artifact to tidy a count is not a trade worth making. |
| B-27 | **RESOLVED.** EvidenceSource identity now follows the ARTIFACT, not its position in a sorted glob. `build_sources` looks up an existing recorded source by `(repositoryPath, sourceType, contentHash)` — the same triple `write_sources` already verifies before reuse — and keeps whatever id that source was given, under any scheme. Only a genuinely new artifact is allocated an id, and it takes the next sequence not already used that day rather than a positional one. No schema change and no id migration were needed: on the live corpus all 17 artifacts resolve to their existing recorded ids and **zero** new ids are minted, so no observation citation moves and no historical evidence is reinterpreted. Proven on the real corpus: removing a capture file moves **0** identities and restoring it reproduces the mapping exactly — the sequence that blocked the importer twice. 7 fixtures, mutations 5/5 killed, including reintroducing the old positional scheme (kills 5). The fail-closed refusal in `write_sources` is retained deliberately: it was correct, and it is what surfaced this. |
| B-28 | **One EvidenceSource points at a capture artifact that no longer exists.** `EVSRC\|MOGO\|20260818\|015` records `repositoryPath: evidence/FWD-20260818T153216Z-PACKAGES.json`, removed during a duplicate-artifact cleanup. Surfaced by the new `UNRESOLVABLE_ARTIFACT` validator rule, which is why it is now visible at all — the validator never inspected `repositoryPath` before, so this passed cleanly for a day. **The evidence itself is intact**: `TOBS\|MOGO\|20260818\|001` is undamaged, and the identical package content (`contentHash f31505ff…`, `PKG\|current_strategy\|20260818\|1`) survives at `evidence/FWD-20260818T153243Z-PACKAGES.json` under `EVSRC\|MOGO\|20260818\|016`. Deliberately NOT repaired by editing the record: repointing a `repositoryPath` that an observation already cites is exactly what `write_sources` refuses to do, and doing it by hand would be a silent reinterpretation of preserved provenance. Restoring the file is now safe under B-27 (identity follows the artifact, so no ids shift) but would put one package in two capture files, which is its own invariant violation. Left visible as a WARNING rather than hidden; resolving it is an owner call between restoring the artifact and superseding the source record. |
| B-29 | **RESOLVED without migrating identities — migration was assessed and REJECTED.** `observationId` is embedded in 259 preserved records and cited across the corpus, so a content-derived id would have meant renaming preserved evidence to make a migration convenient. The existing mechanism was also already bounded: `imported` is contentHash-keyed so a recorded observation is never re-minted, each date's counter continues from the recorded maximum, and `write_observation` refuses to overwrite — no CITED identity could move. What was genuinely unguarded was WHICH sequence two same-date PENDING packages received, because assignment followed file-discovery order and reversing the glob passed the whole suite. Fixed by sorting the pending set on `(createdAt, contentHash)` — a key the package carries — so ordering depends on content, not on the directory listing, and not one recorded id changed. 11 fixtures across the full attack surface (reorder, insert, delete, re-split across files, duplicate content, same content in different sources, >999 sequence boundary, reconstructed-vs-forward isolation, idempotence, retry, already-recorded). Mutations 6/6 killed, including reverting to file order and sorting by path. |
| B-30 | **RESOLVED. One package's content could be minted as TWO observations within a single import run.** Found while building B-29's adversarial tests. `imported` catches a package already RECORDED and `seen` catches an observationId collision — but two packages carrying the same `contentHash` in one run receive DIFFERENT sequence numbers, so neither check fired and both were written. This is the duplicate-mint defect that already reached the live corpus once (two capture files holding one package); it was fixed then in the pipeline's novelty check, while the importer — the authoritative gate — still allowed it. Within-run dedup is now keyed on `contentHash`, skipping with `DUPLICATE_CONTENT_HASH`. Verified on the live corpus: all 259 observation ids and all source content hashes are unique. |
| B-28 | **RESOLVED without repointing anything.** `EVSRC|MOGO|20260818|015` cited `evidence/FWD-20260818T153216Z-PACKAGES.json`, which does not exist; `TOBS|MOGO|20260818|001` cites that source, while `|016` — pointing at the file that DOES exist — was cited by nothing. Diagnosis: a DRY RUN wrote the first capture file, a source was registered for it, the dry-run cleanup (added to stop double-staging) deleted it, and the `--write` run 27 seconds later produced the surviving artifact. The two are byte-identical — both records carry contentHash `f31505ff…`, and re-hashing the surviving file reproduces it exactly, so the evidence the observation depends on is provably intact at another path. Fixed by recording `artifactSupersededBy` on 015 as a new fact ABOUT the record: `repositoryPath` is deliberately LEFT pointing at the deleted file, because it records what that source actually described and repointing it would assert the source was always about the other artifact. The validator resolves the warning only when the named source exists, ITS artifact exists, and the contentHashes are EQUAL — because a supersession field is also a new way to launder broken provenance, and pointing a dangling source at any healthy one would otherwise silence the warning while the described evidence stayed lost. A failed supersession is an `UNVERIFIED_SUPERSESSION` **ERROR**, deliberately louder than the WARNING it replaced: a dangling path is a visible gap, a false supersession is a gap wearing a resolution. 8 fixtures incl. a positive control; mutations 4/4 killed (skip the check, skip the hash comparison, downgrade the severity, drop the unregistered-target branch — the last one initially SURVIVED as an equivalent mutant that kept the invariant but produced a misleading diagnostic, so the fixture now pins the message). Evidence validator: 0 findings, down from 1 WARNING. |
| B-32 | **RESOLVED — owner-approved Option 1 (2026-08-19).** `TRADE_OBSERVATION` is now a first-class graph node: all 259 preserved observations appear exactly once, each with one `DERIVED_FROM` edge to the `EvidenceSource` its own record names (`sourceId`, present on 259/259). Orphan accounting, stated correctly after an independent verifier caught it wrong: there were **48** ORPHAN_NODE warnings (47 EVIDENCE_SOURCE + 1 OWNER_DECISION). Observations cite only **17 distinct** sources, so representing that provenance removed **17** false warnings, not 47; **30** genuinely uncited sources remain and are real. The original "47 false orphans removed" was arithmetically impossible — 47 + 30 exceeds the 59 sources that exist. The mechanism claim was sound; the magnitude was not. Investigated rather than assumed: all 548 packages inside those 30 sources are already preserved as observations under another source, so **no evidence is lost** — they are redundant source registrations, and they must stay visible. **The substantive decision was which edge NOT to create.** Every observation carries `strategyId=alex_g_sr_v1` and `SF|ALEX_G|SUPPORT_RESISTANCE_V1` exists; joining them would assert MOGO's IMPLEMENTATION and a human trader are the same subject, letting a query count OBSERVED paper trades as evidence for a SOURCE_STATED claim. No trader, strategy-family, claim or hypothesis edge is derived — zero records anywhere reference an `observationId` (measured), so the claim→observation link is left ABSENT, which is the correct representation of UNKNOWN (lineage assessment: `SPEC-provenance.md` §7). The direction table caught the new edge on the first build and the pair was enumerated explicitly rather than loosening the gate. New read-only diagnostic `observation_graph_reconcile.py` reconciles counts, hashes, provenance coverage, populations, orphan split, fabricated edges, dangling edges and determinism. Live: 259/259 covered, 0 wrong-target, 0 duplicates, 0 hash mismatches, populations 221/29/9 unchanged, deterministic rebuild, 0 build errors. 20 fixtures incl. positive AND negative orphan controls; mutations 5/5 killed (drop discovery, drop the edge, FABRICATE the strategy-family edge, cross the edges, surface `outcome` as node status). Also fixed, and it is the more durable half: the scratch-tree cleaner list was duplicated BY HAND into three test files, so adding a collection to discovery without adding it to all three seeded every fixture with 259 production observations whose sources had just been deleted — 6 failures across `test_evidence` and `test_phase7a`, and exactly the contamination those cleaners' own docstrings warn about. The entity part of the list is now DERIVED from `graph_common.EVIDENCE_ENTITY_COLLECTIONS` (lifted out of `discover_entities`, so it cannot drift from what discovery actually reads); only the four non-entity collections stay hand-listed. A future node type cannot be forgotten in three places again. |
| B-32.1 | **RESOLVED — round-2 adversarial findings on the B-32 repairs.** Round 1 was "verifying a copy of a check is not verifying the check"; round 2 was its sequel, **"verifying a check is not verifying that anything CALLS it"**: deleting the single call site of `check_observation_population_rebinding` from `run_integrity_checks` left the whole 1124-test suite green, because every rebinding test called the function directly. The published `integrity-report.json` would silently stop reporting the corpus's most consequential failure. Fixed with `TestPopulationChecksAreWiredIntoTheValidator`, which goes through `run_integrity_checks` on a self-written corpus. **Four evasions of the rebinding check were demonstrated on a corpus copy** and all are now closed: deleting the mint stamp, prepending a decoy stamp (`.search` took the first match), blanking `sourceType` on a cited SOURCE (24 observations fell into UNKNOWN with zero findings), and blinding every stamp then retyping one source (**24 replay trades entered FORWARD**, 29→53, everything green). Root cause in every case: the check **failed open** — silence meant "could not tell", not "checked and fine". It now fails closed with four conditions: `POPULATION_REBINDING` (ERROR), `UNRESOLVED_POPULATION` (ERROR), `MISSING_MINT_PROVENANCE` (WARNING) and `AMBIGUOUS_MINT_PROVENANCE` (ERROR); the regex tolerates spacing/case so a stamp cannot hide behind a reader that skips it, and a non-string `notes` no longer raises `TypeError` and abort the entire validator run. **A bug in the first repair was found by re-running the attack rather than by any fixture:** the unresolved-population guard read `population not in POPULATIONS`, but `UNKNOWN` **is** a member of `POPULATIONS`, so it could never fire. Also fixed: `main()` gated the exit code on FATAL only, so 24 rebinding ERRORs exited 0 and no CI could gate on the repair (`exit_code_for`, WARNINGs deliberately still pass); `observation_graph_reconcile.py` printed RECONCILED on an **empty** corpus — eight checks over zero rows, the exact vacuous-loop anti-pattern — and now fails closed, and it cross-checks the production orphan result against an independent recount so a *disarmed* orphan check is itself a finding rather than an answer it faithfully repeats; `_sorted_glob` with `sorted()` deleted survived because this filesystem returns lexical order anyway, so the test now substitutes a reverse-ordering glob; and `population_fidelity`'s `REPLAY_IS_BAR_QUANTIZED` asserted "every forward fill is sub-second" on a `> 0` condition, so it kept asserting *every* about a contaminated population — now guarded on ALL, making it an independent detector of the contamination. Mutations: the round-2 survivors (unwire, `sorted()` deletion) plus the FATAL-only gate and the `not in POPULATIONS` regression are all killed. **A defect in my own verification harness was caught mid-repair:** it passed the repo root where `run_integrity_checks` expects `evidence_root`, so it loaded zero records and four attacks appeared to succeed against the fixed code — every attack rerun now asserts the corpus is non-empty first. |
| B-32.2 | **RESOLVED — round-3 adversarial findings.** Round 3 ran 16 mutations with **zero survivors** — every prior survivor and all four documented population evasions are caught — but REFUTED the claim on two counts. **(F1) A fifth evasion:** repoint 24 observations at a `paper_trade` source **and rewrite their `sourceType=` stamp to agree**. The two sides then match, 24 replay trades sit in FORWARD (221/29/9 → 197/53/9), and every tool is green. Closed using data already in the record and previously discarded: `notes` carries a **second** stamp, `captureBasis=`, which the importer maps 1:1 onto a sourceType — measured 221 REPLAY_RUN / 29 LIVE_CLOSE / 9 HISTORICAL_BACKFILL, zero missing, zero contradictions, so the new `CAPTURE_BASIS_CONTRADICTS_SOURCE` ERROR costs no false positives. Honestly scoped as defence in depth, not proof: both stamps live in one field, so a thorough enough rewrite still defeats it — what it removes is the cheap version. The mapping is imported from the importer rather than restated. **(F2) An observation with no `sourceId`** left its population silently: the code fell through on `source is None` with a comment claiming it was "already reported as a missing reference elsewhere" — true for an id naming a nonexistent source, **false** for an absent, blank or unhashable one, where there is no dangling reference to catch. Deleting `sourceId` from 24 observations moved them to UNKNOWN with the validator silent, and **a test of mine positively asserted that silence**, enshrining the hole; that test's scope is now stated and the validator reports `UNRESOLVED_POPULATION`. **(F5)** An unhashable `sourceId` raised `TypeError` and aborted the ENTIRE validator run, losing every finding including the population ones — the same hardening already applied to `notes` had not been carried to the id. **(F4)** The orphan cross-check could be disarmed on BOTH sides consistently (the witness re-implements the same rule over the same edges, so it is a second implementation rather than a second opinion) — added a floor derived from the RECORDS: a source referenced by no observation and no evidence item MUST come out orphaned however the graph was built, so a graph-side edit cannot hide it. Verified: double-disarm now reports 30 unreported orphans and exits 1. **(F6/F7)** Two overstatements corrected — the reconciliation banner read as a soundness verdict when it cannot detect a DELETED record or an altered `outcome`/`rMultiple` (now says STRUCTURE ONLY and names both limits), and "every observation carries `strategyId=alex_g_sr_v1`" was 257 of 259. Mutations on the new repairs: 5/5 killed, including report-everything, caught only by the positive control. **Round 3 also reported the pristine corpus exits 1 with 46 warnings — NOT reproducible here (0/0/0/0, exit 0).** Cause: `evidence/` is gitignored, so in a fresh worktree B-28's supersession target is absent and correctly errors. Environmental, not a corpus defect, but it means the evidence validator's verdict depends on gitignored artifacts being present. |
| B-32.3 | **RESOLVED — round-4 adversarial findings.** Round 4 found **three surviving mutations**, all in code the previous rounds had ADDED, plus a seventh evasion class with five variants. **(S1)** Deleting `exit_code_for`'s call from `main()` left the whole suite green — `exit_code_for` was unit-tested four ways and nothing asserted the CLI USES it, so a contaminated corpus exited 0 and the new `run_all.sh` gate passed. Third recurrence of one shape: testing a copy of a check, then a check nothing calls, now a helper the entry point need not use. Fixed with a subprocess test of the real CLI plus a positive control. **(S2/S3)** `observation_graph_reconcile.py` had **zero unit tests** — its record-derived orphan floor and its empty-corpus vacuity guard could each be deleted with the suite green. Fixed with a new 12-test module building scratch corpora. **(A7 a–e)** Five ways to move 24 replay observations into FORWARD: rewriting BOTH `notes` stamps consistently; a **decoy `captureBasis=`** (that stamp used `.search`, so first-match-won, while the `sourceType` stamp already used findall+ambiguity — the same attack caught on one stamp and uncaught on the other); a **mangled `REPLAY-RUN`** (the `[A-Za-z_]+` class truncated it to `REPLAY`, which maps to nothing and silently no-opped, so one hyphen disabled the check while `sourceType` failed CLOSED on an unreadable stamp); **retyping a source in place** (no observation edit at all); and **fabricating new records**. All closed except the last two classes by making the two stamps symmetric and adding two anchors that live on the SOURCE, where an observation edit cannot reach: `metadata.captureBasis` (47/59 sources, 0 disagreements) and `metadata.engineStrategyId` (**259/259 agreement with the observation's `strategyId`, 0 disagreements**) — both previously written and never read. Also fixed: reconcile passed `now=""` into a helper calling `now.strftime()`, so the first population finding aborted it at check 4 and checks 5–8 (orphan floor, witness, fabricated-edge, determinism) never ran. **Two limits recorded rather than engineered away** (`SPEC-provenance.md` §7.4): a wholly fabricated record and a coordinated rewrite of all four anchors are undetectable by consistency checking — they are self-consistent by construction. Detection belongs to the corpus fingerprint, the learning ledger and git history. Consistency checks establish that the corpus does not contradict itself, not that it is true. Mutations on the round-4 repairs: 7/7 killed. |
| B-32.4 | **RESOLVED — round-5 adversarial findings.** Six findings, two of them P1. **(1) The tenth and cheapest evasion:** the engine anchor read `if engine and strategyId and engine != strategyId`, so **deleting one field** made it evaluate False and vanish — 24 replay observations entered FORWARD with every tool green and the headline forward mean-R **flipped sign** (-0.179 → +0.015). Now fails closed with `MISSING_STRATEGY_ATTRIBUTION`. **(2) `graph_common`'s trader-edge exclusion was a DATA ASSUMPTION, not a builder rule** — `TRADE_OBSERVATION` was not in the exclusion list, and the fabrication guarantee held only because no preserved record happens to carry a `traderId`. Adding one to a single record built a real `BELONGS_TO_TRADER` edge putting MOGO's paper trades three hops from a human trader's evidence — the precise contamination CLAUDE.md names. The exclusion is now a builder rule and the fixture deliberately CARRIES a `traderId`. **(3) M10:** anchor 3's call site was untested (the check was unit-tested, its wiring was not) — **third recurrence of one shape**, so both source-side anchors now have wiring tests. **(4)** Anchor 3 kept the exact one-hyphen defect repaired on the observation side: `REPLAY-RUN`, a trailing space, or an unknown basis silently no-opped. **(5)** A well-formed `sourceId` naming a **deleted** source left 24 observations in UNKNOWN with the validator silent — it deferred to "the reference checks", which live in the graph build and never reach `integrity-report.json`; a test of mine asserted that silence. **(6)** `check_orphans` runs FIRST and used `item["sourceId"]`, so one malformed EvidenceItem aborted the entire run before any population check — **fourth instance** of a defect already fixed for `notes`, `sourceId` and `metadata`. Mutations 6/6 killed. **A claim of mine was refuted and is corrected in `SPEC-provenance.md` §7.4:** "every attack that edits a preserved record now contradicts at least one anchor" was false. A third residual limit is now listed explicitly — an observation rewritten wholly consistent with a DIFFERENT legitimate source needs no source edit at all, and closing it would need an anchor tying an observation to the package it was minted from, which the corpus does not carry. |
| B-32.5 | **RESOLVED — round-6 adversarial findings.** Two CRITICAL survivors, both shape (e) — a guarantee resting on what the corpus happens to contain rather than on a rule. **(1) Two hops.** B-32.4 excluded `TRADE_OBSERVATION` from `BELONGS_TO_TRADER`, and the comment claimed this made it "a BUILDER RULE, not a data assumption" — true for the observation's own field, **false one hop out**. `EVIDENCE_SOURCE` is still in that allowlist and every observation has `DERIVED_FROM → its source`, so `traderId` on ONE cited source produced `observation → source → ALEX_G` with all four gates silent. The guarantee is now a **reachability invariant** (`check_observation_trader_isolation`): undirected walk from every observation, ERROR naming the path if it reaches any TRADER or STRATEGY_FAMILY at ANY hop — which covers edge types nobody has invented yet, as per-node-type exclusions never could. **(2) The attribute, not the edge.** The round-5 edge fix held (zero edges) while the node kept the raw `traderId`, and `query_graph.known_facts_about_trader` filters on **the attribute** — one field on one record returned MOGO's own paper trade as a known fact about a human trader. Observation nodes now carry neither attribution field; verified against the shipped query (360 rows for ALEX_G, zero observations, vs 361 with one leaked). A positive control blocks the over-correction — blanking attribution for every node type would satisfy both new tests and destroy the graph's ability to attribute anything. **A fourth recurrence of the untested-wiring shape was caught by mutation:** unwiring the new contamination check passed every test, because the tests call it directly — the AST wiring invariant covered `validate_evidence.py` only. It now covers **both** validator modules (49 checks), since a per-module invariant has the same flaw as a per-check test. Mutations 4/4 killed. **Two self-inflicted incidents recorded:** an interrupted mutation run left `return 0` applied in `main()` — the CLI exit-code bypass, live in the working tree, found by verifying rather than assuming the restore had run; and a `git checkout --` restore reverted two **uncommitted** fixes mid-run. Fixes are now committed BEFORE mutation testing, and mutation restores use `git checkout --` only on committed files. |
| B-32.6 | **RESOLVED — round-7 adversarial findings.** Four survivors, three P1. **(1) The fourth population anchor failed open on the SOURCE side.** The observation side had been hardened so deleting `strategyId` could not disable it; the other record was untouched, so deleting `metadata.engineStrategyId` from **one source** moved 24 replay observations into FORWARD with zero findings and exit 0 — *hardening one side of a two-sided comparison is hardening neither*. Reframed as claim-and-corroboration (`MISSING_SOURCE_ATTRIBUTION`), scoped to `engineStrategyId` alone so it does not duplicate an anchor another check owns. **(2) Two validators exited 0 on ERRORs** — `validate_graph` reported 24 contamination ERRORs and exited 0; `validate_acquisition` did the same on an invalid platform. Both hand-rolled `return 0 if FATAL == 0`, the exact defect `validate_evidence` documents having repaired and never shared. `exit_code_for` is now canonical in `graph_common` and used by all three. **(3) The contamination guarantee had NO CI gate at all** — `run_all.sh` ran neither validator; both are wired in now, which only became meaningful once they stopped exiting 0. **(4) Fifth recurrence of the wiring shape, inside the fix for the fourth:** the AST invariant listed both modules but only ONE of its three tests iterated the list, so the contamination check could still be called with a throwaway `findings` list — running and discarding its own findings with every test green. All three tests now iterate all three modules. **A claim of mine was falsified verbatim** ("removes the freedom to repoint at ANY forward source") and is corrected in place. Mutations 5/5 killed. **Fixture fallout handled the right way round:** the new check fired across 28 tests whose synthetic sources carried no attribution metadata — modelling a source MOGO's importer never produces. The fixtures were made realistic rather than the check made lenient, and one expectation was deliberately reversed (a source whose metadata cannot be read corroborates nothing and must not be silent). |
| B-32.7 | **RESOLVED — round-8 adversarial findings.** **(P1) The second anchor was defeated by DELETION.** An absent `sourceType=` stamp was an ERROR; an absent `captureBasis=` stamp was **not reported at all** — so deleting it (24 observation files, zero source files) put 24 replay observations into FORWARD with every gate exit 0 and the forward mean R flipped −0.179 → +0.015. Blanking the value *was* caught, but by a **regex bleed** (the pattern ate the space and captured the next key name) — an accident, not a designed path. Now `MISSING_CAPTURE_BASIS` (ERROR), and the source side too: its docstring justified skipping unstamped sources as "12 predate it", which was false — all 12 are `transcript` sources the importer never stamps and no observation cites, so requiring it on observation-bearing types costs zero false positives. **(P2) Four contamination mutations survived their own test class:** dropping STRATEGY_FAMILY from the forbidden set passed because the walk continued THROUGH the family to a TRADER the same fixture wrote (the test verified an adjacent path, not its named property); caps at 2 and 3 hops were invisible because every fixture reached a trader in exactly 2; and dropping the reverse adjacency was invisible because every real path runs forward, so the undirectedness the docstring asserts was never exercised. Now covered by a family with no trader in reach, a path reachable ONLY backwards, and a **six-hop chain** — deliberately deeper than any plausible cap, since a depth-N fixture only kills caps below N. **(P2) The fixture enrichment DID weaken a control** — the exact risk flagged to the verifier. Re-gating `MISSING_STRATEGY_ATTRIBUTION` survived everything because all 16 `engineStrategyId` occurrences in tests are truthy and none omits it, making gated and ungated indistinguishable. **(P3)** A fourth hand-rolled exit gate in `build_graph.py`, outside an invariant that only looked at modules with `check_*` functions; a second, broader module list now covers status-deciding modules. **Two self-caught follow-ons:** the P1 fix shipped with **no test** (mutation testing found it within minutes), and writing that test exposed that a record missing BOTH stamps reported only one, because the first branch `continue`d — both absences are now evaluated first. Mutations 6/6 killed. |
| B-32.8 | **RESOLVED — round-9 adversarial findings, including a defect CLASS no round had touched.** **(P0/P1) Evidence DELETION was undetected by every gate.** Nine rounds attacked rewriting; nobody tried removal. Deleting the 21 **losing** FORWARD observations moved the headline forward mean R from −0.18 to **+2.01** with all three validators exiting 0 and the WARNING count unchanged at 31; deleting the entire corpus raised only orphan WARNINGs, which deliberately do not fail; renaming to `.bak` or moving into a subdirectory was equally silent. The anchor already existed and nothing read it — every assimilation writes a committed ledger entry recording `observationTotalAfter`, and evidence is append-only by design. `check_corpus_is_append_only` now reports `EVIDENCE_REMOVED` (ERROR) naming how many are gone, against the **maximum** ever recorded (not the latest, or deleting then assimilating once would launder it). Caught while writing it: the check first read the LIVE ledger from a module constant, so every scratch corpus inherited the real high-water mark and a 0-observation fixture "lost" 259 records — a gate reporting on a corpus other than the one it was handed is worse than no gate. **(P2)** `MISSING_SOURCE_CAPTURE_BASIS` required the stamp on "types the importer produces" when the property that matters is "types that BEAR A POPULATION" — `generated_analysis` and `live_trade_review` are population-bearing non-importer types, so one retype plus one deletion shed the anchor. Now derived from `trade_observation`'s own population tuples. **(P3, shape (a) sixth instance)** `test_a_THREE_hop_path_is_caught` never exercised three hops — its bridge was a STRATEGY_FAMILY, itself forbidden, so the walk reported at hop 2, and its own assertion admitted it by accepting "3 hop" OR "2 hop". Rebuilt with a non-forbidden bridge. **(P3)** A docstring still claimed "12 predate it" ten lines above an inline comment saying that was wrong. Mutations 4/4 killed after adding the one test a survivor exposed. **`SPEC-provenance.md` §7.4 updated twice:** deletion removed from the blind spots (now detected), and limit (b) restated as **cheaper than first claimed** — three field edits across two records, not "all four stamps", because a retyped replay source already carries a matching `engineStrategyId`. |
| B-32.9 | **RESOLVED — round-10 adversarial findings.** **The sharpest lesson yet: a repair that anchors on an artifact inherits that artifact's security.** B-32.8's `EVIDENCE_REMOVED` did not make deletion harder — it **moved the target** from 259 observation records to a six-file ledger directory no validator inspected. **Six one-touch bypasses**, each deleting the 21 losing FORWARD observations and moving the headline mean R from −0.18 to +2.01 at exit 0: `rm` the ledger; subtract 21 from every total; corrupt entries to non-JSON; make totals strings; rename `*.json`→`*.jsonl`; delete the field. The check's own defensive guards (`except/continue`, `isinstance`) made half of them free. **DELETE-AND-PAD**, invisible to a count anchor by construction: delete the 21 losers, then `cp` a WINNING observation 21 times changing only `observationId` — count returns to exactly 259, forward n stays at 29 (the one number a reader sanity-checks), mean R lands at +2.00, nothing authored. Cheaper than SPEC §7.4's "fabricated whole record". **Both closed with data the corpus already carried and nothing read:** `DUPLICATE_SEQUENCE_ID` (unique across all 259, none missing — the engine's own decision id, so two records sharing one means one decision recorded twice) and `CORPUS_CONTENT_DIVERGED`, comparing the recomputed `corpus_fingerprint` against `research-state/current-state.json` — **a different committed file from the ledger**, which SPEC §7.4 already NAMED as the backstop while grep showed it was written by `research_assimilation` and read by nothing. The comparison is conditional on the count, which is what makes it false-positive-free: growth legitimately changes the fingerprint, but the SAME count with a DIFFERENT fingerprint did not grow. Also fixed a false-positive vector round 10 found — one fabricated ledger entry claiming 999999 failed a pristine corpus; when state verifies the corpus exactly, a ledger claiming more is now `LEDGER_DISAGREES_WITH_STATE` (still ERROR, still fails closed) rather than sending the reader hunting for files never deleted. All five round-10 attacks verified caught; mutations 4/4 killed; 15 fixtures written before the commit. |
| B-32.10 | **RESOLVED — round-11 adversarial findings. The round-10 lesson repeated verbatim on the round-10 repair.** Anchoring on `current-state.json` moved the attack surface to that file — **twelve** one-touch bypasses, worse than the ledger's six, because both anchors live in the SAME directory: **one `rm -rf research-state/` defeated round 9 and round 10 together**, restoring the original deletion defect in full (21 records gone, mean R −0.18 → +2.01, exit 0). Also: `observationTotal` as a string/null/float/absent, `corpusFingerprint` absent, and **decrementing the recorded total by one** — which makes the count-conditional comparison read the corpus as "grown" and skip content checking entirely. **`sequenceId` was a free pass:** the gate skipped any non-string, so padding needed no fresh ids — just DELETE the field (5 variants all walked through). Replaced by `DUPLICATE_SOURCE_CONTENT_HASH` on `sourceContentHash` — present and distinct on all 259, and unlike a free-text label it is the hash of the preserved package, so freshening it means hashing bytes that must also exist, which is exactly the authoring cost delete-and-pad avoids. Missing state is now `RESEARCH_STATE_MISSING` (ERROR): "silent when absent" and "silent when misconfigured" are one bug seen from two sides — five of round 11's eight survivors were gates unwired or **aimed at a nonexistent path** with the suite green, closed by wired-AND-aimed tests through `run_integrity_checks`. **NEW CATEGORY — launder-by-reassimilation:** tamper, then run the ordinary `research_assimilation.py --write`, which re-stamps the fingerprint from whatever is on disk; no file edited, no hash forged, one documented maintenance command. Now blocked upstream — the tamper itself trips `DUPLICATE_SOURCE_CONTENT_HASH`, which laundering cannot erase. **A proposed gate was measured and REJECTED** (`SPEC-provenance.md` §7.5): the ledger's same-count-fingerprint-change signature was called "structurally free of false positives", but 3 of 6 real entries carry it legitimately (B-28, B-22). A legitimate edit and a laundered substitution are structurally identical, so the ledger is a forensic record, not a gate — and a false gate is worse than a documented limit. All four round-11 headline bypasses verified caught. |
| B-32.11 | **RESOLVED — round-12 findings, and the shape finally given a systemic invariant.** Round 12's verdict: **no new category**, but the stopping rule unmet, because ONE shape had been repaired four rounds running instance-by-instance — *a gate that returns silently when what it reads is missing*. It found the next two doors: keep `current-state.json` but delete the `observationTotal` **key**, and delete the ledger **directory** — two edits, pure `rm` of the 21 losing forward observations, mean R −0.18 → +2.01, exit 0. 8 of 12 state variants were still silent and a stub `{}` dodged `RESEARCH_STATE_MISSING`. **Patched as an invariant, not a fifth door:** `check_corpus_anchors_are_available` checks availability ONCE from a declared table (state file, both fields with correct types, non-empty ledger dir), covering the attacker's "delete what it reads" and the maintainer's "aim it at nothing" as the same bug. Plus **cross-anchor agreement** — an anchor can be present, correctly typed and lying, so a state total below the ledger's high-water mark is `STATE_CONTRADICTS_LEDGER`. All 13 variants close (11 by availability, 2 by cross-anchor). **Severity now asserted WITH the exit code**, table-driven: downgrading a gate to WARNING left it wired, reporting, and non-blocking with every `findingType` assertion passing. **A claim of mine was false and is corrected in place:** `sourceContentHash` "cannot be freshened without hashing bytes that must exist" and laundering was "blocked upstream" — nothing compares it to anything (0/259 equal their source's `contentHash`; different granularities), forging costs one character. It is a **duplicate detector, not an authoring-cost barrier**; SPEC §7.4 said so and my docstring contradicted it. Mutations 6/6 killed, including an equivalent mutant closed by pinning the diagnostic. **Process failure recorded (3rd occurrence):** restoring mutations with `git checkout --` reverted uncommitted fixes again — restores now use a scratch copy. |
| B-32.12 | **RESOLVED — round-13 found a FIFTH CATEGORY, the falsifying outcome round 12 named in advance.** **The shape: append-only was enforced in AGGREGATE, never per identity.** Every gate from eleven rounds reduces the corpus to a cardinality plus a whole-corpus hash pinned in a file the same actor can rewrite; none ever asked WHICH observations have existed. So a count-preserving substitution was invisible to all of them at once — delete the 21 losing forward trades, pad with 21 copies of a winner, count stays 259, forward n stays 29, mean R −0.18 → +2.00. **And the system re-blesses it:** `research_assimilation.py --write` re-stamps `corpusFingerprint` from whatever is on disk, so one documented maintenance command launders the tamper and leaves a ledger showing a normal append with an intact chain — falsifying `check_corpus_is_append_only`'s claim that it would "show plainly in the ledger diff". **The anchor was already on disk, committed, and unread — third time this exercise.** `evidence/ledger-preservation/` records the PAPER account's closed trades by `tradeId`, which IS the observation's `sequenceId`, and is git-tracked rather than gitignored: 39 identities, 35 present, the 4 absent all `AGT|TEST|` developer trades the importer refuses (the manifest itself counts `closedDeveloperTest: 4`), so requiring every non-developer identity costs zero false positives. **An aggregate anchor can always be recomputed from the tampered corpus; a per-identity record cannot, because it records what was there BEFORE** — which is why this one alone survives laundering. All three round-13 survivors (D3, F9, G1) now report 19 `PRESERVED_IDENTITY_MISSING`, exit 1, where all three exited 0. Also closed **in the order round 13 warned was load-bearing**: a non-dict state document made the anchor invariant silent then crashed at three sites, leaving a stale green report on disk — fail-closed had to land BEFORE the crash fix, or fixing the crash converts a loud failure into a silent pass; a JSON `null` needed a sentinel to be told from "never loaded". Mutations 6/6 killed after a survivor exposed the missing non-dict test. **`SPEC-provenance.md` §7.4 corrected** (it claimed nothing in the corpus maps identity — true of packages, false generally) and **§7.6 added**: *a defence assembled by repairing the last attack converges on the last attack; ask not "does this close what I just saw" but "what does this gate reduce the corpus to, and what is invisible in those terms?"* |
| B-32.13 | **RESOLVED — round-14: the lesson executed on the commit that cited it.** The identity gate written in B-32.12 opened with `if not preservation_dir ...: return` — the exact fail-open shape rounds 9–12 spent four rounds eliminating — and was **not in `CORPUS_ANCHORS`**, the table whose own comment says availability belongs there. **Eleven one-touch bypasses**, each restoring full exit-0 substitution (forward mean R −0.18 → +2.04): remove the directory, empty it, rename the extension, move it to a subdirectory, make the document `null` or a list, make rows strings, rename `tradeId` or `identities`, or re-prefix every id `AGT|TEST|`. The `null` case is the same JSON-null hole fixed with a sentinel *one function above, in the same commit*. **A test of mine asserted the first bypass was correct behaviour** — replaced, not amended. Availability now covers this anchor with manifest-shape checks including *no requirable identities* (a manifest of all-developer rows is non-empty yet requires nothing). Scoped to corpora holding FORWARD evidence, since the manifest records PAPER closed trades. Two further self-inflicted bugs found by mutation: the scoping was first an early `return` that silently disabled the unrelated cross-anchor check, and the preservation check sat *after* the ledger's early returns so a missing ledger disabled it — **one gate's absence disabling another, the third occurrence**. Wiring tests initially passed on an *adjacent effect* (any anchor missing produces the same finding type) and now assert which anchor the message names. All 11 bypasses exit 1; mutations 4/4 killed. |
| B-32.14 | **RESOLVED — owner option (A): continuous identity coverage from MOGO's own origin, 13.5% → 100%.** The identities were already inside MOGO's IndexedDB origin: every evidence package carries `sourceTradeId`, and the capture pipeline already extracts hash-verified packages origin-scoped. `identity_manifest.py` commits that stream as an append-only record; `forward_capture.sh` updates it on every `--write` run, **before** the novelty check and the "nothing new" early exit, driven by the FULL recovered set — so a quiet capture is an idempotent no-op rather than a gap. **Chrome shared Local Storage is never read** (option B declined; it is one store holding 136 origins of personal browsing). Backfill deterministic from 263 packages → 263 identities, 259/259 observations covered, nothing invented. Not a new single point of trust: identities carry the package `contentHash`, a same-id/different-hash arrival is a CONFLICT (first value kept, no write, nonzero exit), and availability is enforced by `CORPUS_ANCHORS`. Atomic write; damaged manifest REFUSED not reset; retry converges; reordered input byte-identical. Both round-14 coverage attacks now exit 1. |
| B-32.15 | **RESOLVED — the append direction, unlocked by the coverage.** `UNANCHORED_OBSERVATION` (ERROR) reports any observation whose `sequenceId` is in no manifest. The pipeline records identities BEFORE importing, so anything minted from a captured package is anchored by the time it exists. Round 14's fabrication-by-append (200 invented winners, nothing deleted, forward mean R −0.18 → +1.72, previously exit 0) now reports all 200 and exits 1. |
| B-32.16 | **RESOLVED — round-15: two P1s and a new P2 category, all mine.** **(P1-A, NEW CATEGORY: scope-condition laundering.)** The preservation anchor ran only when the corpus held FORWARD observations — a scoping convenience I added for fixture quiet. The require-list, allow-list and availability check all switched off together the moment the corpus held no forward record, **a state the attacker creates by deleting the very evidence the anchor protects**: 29 forward observations destroyed + 200 invented historical winners, historical mean R 0.167 → 1.038, **total silence, exit 0**. Rounds 9–12 closed *delete what the gate READS*; this was *delete what makes the gate APPLY*. **A condition an attacker controls is not a scope, it is a switch.** Scoping removed — the manifest covers every captureBasis including 221 REPLAY_RUN. **(P1-B.)** The allow-list skipped any non-string `sequenceId` "because absence is reported by the sequence-id checks" — no such check existed; both readers skipped non-strings. Deleting one key per fabricated record walked 200 invented winners past it, forward mean R −0.18 → +2.60 — **and a test of mine asserted that was correct**. The shape is documented verbatim earlier in the same file for the same field. **(P2, NEW CATEGORY: over-reporting.)** Every prior round attacked under-reporting. The importer refuses developer trades on three markers; a manifest row has no position object, so re-deriving from the id saw one of three — a developer trade without the prefix would be required forever and never satisfiable. Refusal is now RECORDED at capture time (259 requirable / 4 refused, exactly matching). Also: a conflict WROTE the manifest before reporting, so the anchor advanced on its own failure path. Plus three test defects: five classes defined after `__main__` (direct runs silently ran 22 of 27), the live-coverage assertion filtered falsy ids (the same fail-open inside the test certifying coverage), and `CORPUS_ANCHORS` was decorative — my table test built `reported` by scanning only declared names, so the undeclared direction could never fail. **Mutations 10/10 killed.** |
| B-32.17 | **RESOLVED — round-16: the anchors pinned existence and never value.** Every identity present, every hash intact, every count unchanged, every anchor readable — and `pnl`/`rMultiple`/`outcome` rewritten IN PLACE on the preserved forward losers moved forward mean R from −0.18 to +2.00, **exit 0, total silence**. The documented maintenance command `research_assimilation.py --write` completed it without a single hand-edited state file. The contradicting values were already committed and read by nothing: ledger `pnl`/`pair`/`closedAt` agree 35/35 and manifest `contentHash` agrees 259/259. Built as `ANCHOR_VALUE_BINDINGS` + `ANCHOR_FIELDS_UNBOUND` + `UNADJUDICATED_ANCHOR_FIELD` so a field added to an anchor writer must be adjudicated rather than joining the unread set — the shape was already present twice before anyone looked. Escapes closed: delete the bound field (`ANCHOR_VALUE_UNCHECKABLE`), rename it at the writer (`UNADJUDICATED_ANCHOR_FIELD`), remove all of them (`ANCHOR_VALUES_UNCOMPARED`, counted over rows that JOINED). Also killed the three round-16 mutation survivors: **M4** — the round-15 P1-B repair had zero test coverage, so deleting it broke nothing; **M10** — an empty manifest directory was silent; **M11** — a non-list `identities` was silent, and survived every obvious test by adjacent effect until a healthy manifest was placed beside the corrupted one. **Mutations 10/10 killed.** Residual limit recorded in SPEC §7.9: the manifest has no rollup, and no in-corpus anchor closes hand-appended rows under a write-anything threat model. |
| B-32.18 | **RESOLVED — round-17: a record is now checked against ITSELF, and a document against its rows.** **(P1-A, NEW CATEGORY: intra-record derivability.)** Every gate through B-32.17 compares the corpus to an EXTERNAL witness, so its reach stops where the witness stops. `rMultiple` and `outcome` are bound by no anchor — and `rMultiple` IS the forward-performance headline. Rewriting those two while leaving `pnl` intact, so every §7.9 binding still agreed, moved forward mean R from −0.06 to +2.00 and the win rate 31.4% → 100%; one `research_assimilation.py --write` cleared the only finding raised. The same tamper on the **224 replay observations, which no ledger records and no anchor can ever cover**, was silent outright. Fixed with `RECORD_DERIVATIONS`, measured on the live corpus: rMultiple from prices **259/259, max deviation 4.1e-07**; outcome from sign(R) 259/259; rMultiple from pnl/risk 38/38. No anchor needed, so it covers every cohort and every future close. **My first version skipped a derivation whose inputs were absent — deleting `entry` from 259 records was completely silent. Shape (c), written into the check whose own docstring names it.** `required` is now measured against the live corpus and asserted by a test. **(P1-B.)** `UNADJUDICATED_ANCHOR_FIELD` was row-scoped, so an anchor DOCUMENT's own fields stayed unread — the B-32.17 shape one scope up, in the commit that fixed it. That is why deleting the 24 rows you tampered was silent while deleting all of them was caught. `closedTotal`/`closedReal`/`closedDeveloperTest`/`ledgerRollup` are now derived from the rows (`ledgerRollup` = sha256 of row hashes joined by newline, confirmed against `preserve_paper_ledger.js`); 9 capture-time fields excused with reasons. **(P1-C.)** `BLOCKING` was a hand-kept list, so downgrading `ANCHOR_VALUE_CONTRADICTED` to INFO passed the whole suite — a table that must be remembered is not an invariant. The severity test is now **closed over the source**: all 75 finding types partition into blocking-only / context-dependent / soft-by-design. **(P2.)** A single NaN raised out of `run_integrity_checks` before the report was written, leaving the previous run's `ERROR: 0` on disk for other tooling to read; now `NON_FINITE_VALUE` + `UNSERIALISABLE_CORPUS`, report written, exit 1. **(P3.)** `unittest.main()` sat at line 987 of 3286, so running the file directly executed **77 of 231 tests and printed OK**, skipping every B-32.17/.18 case — it cost a verifier a full false-clean mutation round. Moved to the end. |
| B-32.19 | **RESOLVED — round-18: the record is now checked against the PACKAGE it was minted from.** **(P1, NEW CATEGORY.)** Every gate through B-32.18 compares the corpus to something the corpus also writes — an anchor MOGO produced, a document MOGO stamped, or the record's own arithmetic. So a forgery that is internally consistent and leaves the anchors alone passed all of them: rewriting all 259 observations so each derived R **genuinely is** +2.0 (moving `exitPrice` to match), emptying the ledger rows, then one `research_assimilation.py --write` → **exit 0, forward AND historical mean R 2.0, win share 1.0**. The replay-only variant touched nothing outside `observations/` and was equally silent. **The witness was committed and read by nothing — the fifth recurrence of the shape B-32.17 names in its own words.** `sourceContentHash` resolves to exactly one package (259 distinct hashes, 0 collisions across 263), `repositoryPath` names the artifact, and this validator was already opening that path **to call `os.path.exists` and stop**. Seven fields agree 258/258. Now 163 and 136 `PACKAGE_WITNESS_CONTRADICTED`, and laundering cannot clear them because it never touches the artifacts. **Three documents of mine asserted this was impossible** — SPEC §7.4, the `check_no_two_observations_share_a_package` docstring, and B-32.4/B-32.10 — by conflating *the artifacts are gitignored* (true) with *the corpus carries no package mapping* (false). All corrected in place. Absence is COUNTED at WARNING, not skipped: 1 of 259 has no surviving witness, and silence would make deleting `evidence/` a switch. **(P2.)** The B-32.18 document gate was fail-open on deletion in four ways — `for field in sorted(manifest)` never visits a DELETED bound field, and the rollup was derived only when every row carried a `hash`. "Unmeasurable is reported, not skipped" applied to the record gate and not to the document gate 200 lines away, in the same commit. **(P1-A, my own test defect.)** `risk = abs(entry - stop)` → `entry - stop` **survived all 252 tests** and would fire 127 errors against the live corpus — 49% of it — because the suite's only short fixture inherited `stop` BELOW entry, a long's geometry, making `abs()` a no-op. All 127 real shorts carry the stop above entry. Fixed, plus an assertion that the live corpus geometry still matches what the fixture claims. Also: document bindings are now keyed by `schemaVersion`, because requiring the ledger's counts of the identity manifest reported 4 contradictions against a clean corpus. |
| B-32.20 | **RESOLVED — round-19: the witness could be switched off by DELETION, and the counter that was documented as the tripwire did not move.** `_witness_value` returned a bare `None` both for "the engine recorded nothing" and for "this cannot be read at all", and the caller skipped it — so the record still counted as *witnessed*. A forged `positionSize` (no intra-record derivation, so the package is its only check) plus `objects` stripped from all 263 packages, laundered through `research_assimilation.py --write`, reported **byte-identically to a pristine corpus**. B-32.19's own docstring promised that count would catch exactly this. **The round-15 lesson applied at the package level and not carried one layer down.** Repaired by separating the two cases: structural damage or an absent key is an ERROR (measured 0 of 262), a null where the engine never nulls is an ERROR (0 of 262), a null where it does is COUNTED (2, one LIVE_CLOSE package), and a RECORD that drops a field the package supplies is an ERROR (0 today). `nullable` is measured and asserted against the live packages. Stripping `objects` now reports **1804 errors** where it reported none. **(P2.)** `setdefault` resolved a duplicate `contentHash` by iteration order, silently, and the docstring leaned on "measured: zero collisions" — a snapshot standing in for an invariant. Measurement corrected it twice: **25 of 262 packages DO appear in more than one artifact** (a capture run re-exports what an earlier run wrote), so only a *disagreement* is ambiguous — reporting duplication itself would have been 25 false positives. Also reversed a test of mine that asserted an ambiguous package's silence was correct. |
| B-32.21 | **RESOLVED — round-20: no new category, but a live bypass INSIDE the previous repair.** B-32.20's `nullable` flag took a fact about ONE package (the engine nulls `balanceBefore`/`balanceAfter` on one LIVE_CLOSE capture) and made it a licence covering all 262. Those two fields have no intra-record derivation, so the package is their only check: nulling one key in the artifact and forging the matching record field was **a WARNING and nothing else — 257 records forged, zero errors**. `accountBalanceBefore` feeds `population_fidelity.risk_pct_of_balance` → `RISK_SIZING_AGREES`, so it forges a scientific finding. **The exemption had zero true positives** (it never fired on a clean corpus), and **its test pinned it in one direction** — understating nullability failed, overstating passed, and a mutation flipping `positionSize` to nullable took the same attack from 258 errors to 0. Flag removed: what legitimises a null is that the RECORD claims nothing either, one rule with no list to keep, measured cost zero. Nulling the witness now costs exactly what forging it costs. **(P2.)** The seventh category's own front door — `stated is None: continue` — meant deleting BOTH `rMultiple` and `outcome` skipped every derivation: 161 losing records tampered, forward mean R 0.13 → 2.00, win rate 100%, n falling 259 → 98 because the records stopped counting. Now 322 `RECORD_FIELD_MISSING`. **Third site of the same shape in three commits** (package resolution, witness value, derived field). Also reversed two more tests of mine that encoded the exemption as correct. |
| B-32.22 | **RESOLVED — round-21: the first round that could not break `validate_evidence.py` at all** (7/7 mutations killed, every attack caught) — and found its P2 one level up. `run_all.sh` guards the JS lane against silent shrinkage with a per-runner count manifest; the **Python lane had none**. Its only gate was the exit code of ONE `unittest` call over all modules, and its module count counted FILES — so renaming `test_` to `xtest_` across a whole module de-collected every test in it and the run still exited 0, because sibling modules kept the total non-zero and Python's own `NO TESTS RAN` sentinel never fired. **Twenty-one rounds of hardening are STORED as those tests** — they are what kills every mutation — so the lane holding the entire accumulated kill record was the one with no guard against it shrinking, while the lane holding UI fixtures had one. Added `tests/count_python_tests.py` + `expected_python_test_counts.tsv`, checked both directions, wired into `run_all.sh` and `update_expected_counts.sh` so the lanes cannot diverge in whether they are protected. **The gate has its own suite** — a mutation making `--check` always pass survived everything until it existed, the sixth unwired/unassertable check this milestone. **Two latent FALSE POSITIVES fixed** (a gate that cries wolf gets switched off): `risk_pct_of_balance` used truthiness where its docstring promised presence, so a trade risking exactly zero vanished from the sizing distribution; and outcome-from-R reported a genuine breakeven as `DERIVATION_UNCHECKABLE` — now a distinct NO-VERDICT sentinel, with R-from-price still checking the number so `rMultiple: 0` is no escape. **Plus the round-20 P3 crash**: `_diff_numbers` sorted a `None` group key and raised out of `run_integrity_checks` before the report was written, leaving the previous run's `ERROR: 0` on disk. Three of my own mutations survived first pass, each exposing a real gap — most tellingly that **outcome-from-R was untested for the 221 replay records it exists to cover**, because every fixture carried `pnl` and outcome-from-money caught the forgery instead. **Mutations 11/11 killed.** |
| B-32.23 | **RESOLVED — round-22: the write path, the least-examined surface in the system.** Twenty-two rounds hardened the gates that check records AFTER they exist; almost nothing had examined the code that MINTS them. No P1 and no widened boundary, but two real P2s. **(a) The reconcile step's exit code was discarded.** Every step in `forward_capture.sh` checks rc via `PIPESTATUS` except the last — the gate named "forward population and balance relation" — which piped into `sed` and moved on, so `--write` completed, assimilated and exited 0 whether it passed, failed, or never collected a test. **The same shape repaired one commit earlier in `run_all.sh`, left standing in the chain that runs on live forward evidence.** **(b) The importer's field mapping was pinned by nothing** — 5 mutations survived the whole suite: `entry ← originalStop` (mints a record whose entry equals its stop), `exitPrice ← balanceAfter`, `accountBalanceBefore ← balanceAfter`, `positions[-1]`, and `sequenceId = None`. Not a bypass (the witness catches the records) but the catch is downstream and conditional on a gitignored artifact surviving, and `forward_capture.sh` never runs the validator. Repaired as an **invariant**: the importer's mapping for a witnessed field must BE the pairing the witness compares it by — two tables checked against each other in both directions, plus an end-to-end test that a minted record passes its own witness. **Three more in the append-only record:** `merge` was SHRINKING the manifest by dropping malformed rows while reporting success, in the module whose docstring calls a shrinking manifest the defect it exists to catch; a null `contentHash` was locked in permanently because the conflict predicate needs both hashes truthy, silently voiding that row's stated defence; and a two-position package was silently partially imported against the importer's own contract and the witness's own rule. **Mutations 13/13 killed** (all five survivors plus six harder variants). |
| B-32.24 | **RESOLVED — round-23: no new defect category (second consecutive round), one blocker: B-32.23's shell repair was pinned by NOTHING.** Two mutations neutering the reconcile guard survived the whole suite, and `grep -rn forward_capture tests/` returned one hit — inside a docstring. The shell that runs on live forward evidence had zero coverage. Repaired as the invariant the shape deserves, since it has now been fixed three times with nothing pinning any of it: **structurally**, every failure-capable pipeline must be `if !`-guarded (safe only because `pipefail` is set — also asserted) or capture `PIPESTATUS` into a variable that is tested, with the scan following the VARIABLE rather than a line window; **behaviourally**, the reconcile block's REAL BYTES are extracted from the shipped file and executed against a stub — pass continues, fail stops, collecting nothing stops — because rewriting the guard into the test would verify a copy (defect shape (a)). The premise that `unittest` returns nonzero on NO TESTS RAN is measured, not assumed. **The invariant immediately caught a pre-existing instance**: `IMPORT_RC` defaulted to 0, so removing its assignment would make the import step silently pass — a defensive default that defaults to "fine". All rc defaults now fail closed. **And my first structural test used a 3-line window that my own comment pushed the check past**, failing on a correctly-guarded step — proximity is not the property. **Mutations 8/8 killed**, including one making the scan itself vacuous. **Separately: the auto-mode governance block had decayed** — `soft_deny: 73 installed vs 74 generated; missing: Unverifiable Deletion Scope`. Regenerated (purely additive: +1 shipped deny rule, 0 removals, MOGO's 7 delta rules unchanged, timestamped backup taken). This is the decay CLAUDE.md warns about, and the drift check is the only thing in the system that notices governance rotting by standing still. |
| B-32.25 | **RESOLVED — round-24: no new defect category (THIRD consecutive round). One blocker, and it was in the invariant itself.** B-32.24 asserted "every rc default now fails closed". **False when measured.** The scan matched `[A-Z_]*RC` — the two names I was looking at — and `assimilate_then()`, the function called before EVERY exit of the chain, still read `${rc:-0}`: lowercase, four lines from the top of the file the scan reads. **An invariant scoped to the names you happened to be looking at is an instance patch wearing a table.** Corrected by widening the DEFINITION, not by adding names: defaults are now found by USAGE (a status variable is one assigned from `$?`/`PIPESTATUS`, which is what makes 0 mean "fine" — matching every variable was wrong the other way, since `${REFUSED_COUNT:-0}` is a count), and the command list is **INVERTED** — it listed commands that CAN fail, so a piped `mogo_evidence_checkpoint.sh` (already invoked twice by this script), `env python3`, or `cat f | python3` were unseen. Everything is failure-capable now unless it is a filter, and only constructs whose status THE SHELL consumes are skipped. **Non-vacuity is no longer a count**: the scan is fed lines it must catch and lines it must not — a count cannot tell "found the right three" from "found three". Also: the guard must be tested AFTER its capture, which now fails closed loudly (`exit=1`) rather than silently, precisely because the default is 1. **Mutations 7/7 killed**, including all four command forms round 24 showed the whitelist missed. SPEC §7.18's false sentence corrected in place with the wrong claim left visible. |
| B-32.26 | **RESOLVED — round-25 adjudicated the owner's stop condition MET** (fourth consecutive round with no materially new defect category) and declined to manufacture a blocker. It handed over three fail-open dimensions of §7.19's own scan, none with a live instance in the shipped file, all closed here because leaving them would contradict §7.19 two commits after writing it: **logical lines** (`splitlines()` scanned a different program than bash runs — a continued pipeline is two physical lines, neither failable alone), **quoted status forms** (`EXTRA_RC="$?"` is the same variable with the same meaning), and **captures nobody examines** (`VAR=$(python3 …)` with no test is as unguarded as a bare pipeline — but only when the command can fail and neither the value nor a following `$?` is checked; both refinements came from the scan OVER-FIRING on legitimate code, the failure mode that gets a real scan deleted). **Mutations 8/8 killed** — all three bypasses, the control, and three mutations of the new machinery. Also removed **stale OPEN duplicates of B-32.14/B-32.15 sitting BELOW their RESOLVED rows**, which misled anyone scanning the table for open items about the two most consequential decisions in the campaign. |
| B-32.14 / B-32.15 | *(The OPEN rows that stood here described the state before the owner chose option (A). They were superseded by the RESOLVED rows above and are removed rather than left below them, where a reader scanning for OPEN items was misled about the two most consequential items in the campaign. The history is in the commits and in SPEC §7.7-§7.8.)* |
| B-31 | **RESOLVED. The stranded-close alarm had been silently disarmed.** Four `AGT|TEST|` developer trades were staged in `evidence/FWD-20260819T001808Z-PACKAGES.json`, and the importer refuses them by policy (`DEVELOPER_TEST_TRADE`), so they could never become observations and never clear. Every capture run reported "4 pending" indefinitely. The noise was not the problem: PENDING is the *only* signal that a real forward close was stranded — the exact failure B-22-era work fixed after a GBP/JPY close sat unimported — and a signal with a permanent floor of 4 cannot ring, because the next stranded close reads "5 pending" and looks like the steady state. `classify_recovered` now returns a fourth state, REFUSED, using the importer's own predicate (`is_developer_test_package`, extracted so the pipeline and the gate cannot disagree about what a developer trade is). Refused packages are excluded from `fresh` as well, since staging one creates a capture file that can never import — how the condition arose — and are reported by the pipeline rather than dropped, so a wrong predicate shows up instead of silently deleting a real close from capture. Live pending count is back to 0 (0 fresh / 0 pending / 4 refused). 7 fixtures including two positive controls; mutations 3/3 killed — removing the branch, refusing everything (caught only by the positive controls), and dropping one of the three markers. |

## Governance decisions — deferred, none blocking PAPER

| # | Decision | Blocks paper? |
|---|---|---|
| G-1 | **Widen the protected-function list.** `getStructuralAOI`, `clusterLevels`, `alexGFetchExecutableCandles`, `fetchCandlesAroundWindow`, `runAutoTopDownScan` and the trade-ID seeders all set or gate real prices and sit outside the 63. | No |
| G-2 | **Auto-trade eligibility staleness.** `scanData[pair].bucket` has no age bound; with Auto Trading ON and Auto Scan OFF a days-old `Active watch` still authorises a trade. A TTL changes which trades are taken — frozen semantics. | No — current behaviour preserved |
| G-3 | **Paginator contradiction.** `runHistoricalDataDiagnostic` treats a short page as exhaustion while `fetchCandlesRange` documents that it is not; fixture MDHIST-2 currently blesses the truncation. | No — diagnostics only |
| G-4 | **`exitDetectedAt` fabrication.** `alexGCloseLivePosition` stamps `m.exitDetectedAt||Date.now()`, inventing a plausible detection time when none was supplied. One-line fix, but the function is PROTECTED. Applied, drift tripped, reverted byte-for-byte. | No — evidence timestamp fidelity |
| B-32.22 | **IMPLEMENTED, NOT RUN — the source artifacts leave Git permanently and gain an encrypted operator-local backup instead.** B-32.21 (option A3) force-added the 18 declared capture artifacts so the 48 `storageLocationType: "repository"` declarations would become true. It worked, tested clean, and was **withdrawn by operator ruling**: the artifacts carry **18,397 OANDA-derived OHLC bars across 221 of 268 packages and 11 instruments**, and OANDA's API licence limits that data to Internal Use and prohibits providing Trading System Rates to third parties. Pushing to GitHub would be exactly that. A3 was fully unwound — 18 unstaged one literal path at a time, 7 modified files restored to `afc60ae`, the restored duplicate removed, donor untouched and still `f31505ff…` — and **nothing OANDA-derived was ever committed**. Replaced by ADR-010 amendment A4: Git keeps `ARTIFACT_INDEX.json` (paths, whole-file SHA-256, sizes, package counts, package contentHashes, algorithms, canonicalization id, one generatedAt, and a notice stating the restriction) and nothing else. **A SHA-256 is not rates**, so the verification chain stays public while the data stays operator-local. The index is enforced clean by test rather than by care: no float at any depth (a price would be one), no forbidden field name, no drive name, no path, no backup status, no per-artifact timestamp — mutating a byte size to a float, adding an `instrument` key, or leaking `MOGOTH` each fail. New `scripts/backup_source_artifacts.sh`: `--backup` / `--verify` / `--restore`, each requiring an exact `--dest`, `--confirm`, a terminal on stdin and a typed YES. **No plaintext archive is ever written** — it creates an EMPTY AES-256 `hdiutil` image, attaches it, and copies literal paths in, rather than tarring 18,397 candles to disk first. **The passphrase reaches hdiutil on stdin via `-stdinpass` and nowhere else** — never argv (refused explicitly, with the `ps` reason given), never an env var, file, receipt, history or Keychain. Refuses Time Machine managed directories, the volume root, symlinks, globs, `..`, relative paths, read-only and unmounted volumes; removes a partial image after failure but never a completed backup; no git, no network. 34 tests, 12/12 mutations killed. **Two of my own test defects were caught by mutation, both the same shape — scanning the promise instead of the data:** the index scan flagged the notice sentence saying "no candles, no OHLC values and no prices", and the Time Machine refusal test matched the informational note printed for any destination on a TM volume, so **deleting the guard entirely still passed**. Both tightened to assert the specific refusal. **Four tests are STRUCTURAL, not behavioural** (space exhaustion, partial-image cleanup, archive-corruption detection, restore-refuses-overwrite): exercising them needs a real encrypted image and real OANDA data, which this phase was not authorised to create. They are named `_structural_` so the weaker evidence is visible rather than implied, and should be run behaviourally with synthetic data at first operator-supervised use. **MOGOTH is the Time Machine destination**, so the backup would share one physical device with the system backup — resilience, not independence. A second encrypted device, disconnected and stored separately, is recommended and unauthorised. **No backup image was created and no OANDA package left the Mac.** |
| B-32.23 | **BACKUP EXECUTION: DEFERRED — awaiting separate operator-controlled USB device.** The encrypted source-evidence backup mechanism is **implemented, behaviourally tested, committed and pushed** (`afe5bc3`); **execution remains pending and no backup has been created**. The intended destination `/Volumes/MOGOTH` is unusable without bypassing a protection that should not be bypassed: its volume root carries the Time Machine destination ACL `group:everyone deny add_file,delete,add_subdirectory,...`, owned `root:wheel`, so an ordinary `mkdir` is denied. Writing there would require `sudo` past that ACL, or adding an APFS volume to the same container — both refused by operator ruling, and neither would fix the underlying problem: MOGOTH is the Time Machine destination, so an archive there shares one physical device with the system backup and one drive failure loses both. **Status: DEFERRED — awaiting separate operator-controlled USB device.** Until then the 17 OANDA-derived capture artifacts exist on the working disk only, plus whatever Time Machine holds on MOGOTH (included in TM scope, but recoverability from a snapshot is UNVERIFIED — checking would require mounting one). `git clean -xdf` still destroys them. This is a known, accepted, recorded exposure, not a closed item. |
| G-2 | **REPAIRED (uncommitted, under review) — and the originally reported mechanism was right; an earlier diagnostic of mine was wrong.** I first reported G-2's stated mechanism "disproven", on the reasoning that `checkAutoTrades()` runs inside `scanAll()` immediately after `scanPair()` refreshes every pair. That conflated two different writers. **`scanPair()` writes `pairData` (display/confluence); `runAutoTopDownScan()` writes `scanData[pair].weekly/daily/fh/bucket` — the trade-eligibility state — and it runs SOLELY on `autoScanTimer`, which `toggleAutoScan()` clears when Auto Scan is switched off.** `checkAutoTrades()` lives in the other loop on a different cadence. So with **Auto Trading ON and Auto Scan OFF**, nothing refreshed the bucket or the W/D/H4 states while `checkAutoTrades` kept reading them every sweep: an arbitrarily old `Active watch` plus an arbitrarily old HTF snapshot authorised real paper entries indefinitely. That is G-2 exactly as reported. A second path existed for one pair whose top-down refresh threw mid-sweep — recorded at that catch since v12.36.2 and deliberately left unrepaired there, because clearing the bucket would change which trades the strategy takes. **REPAIR (Design C, no TTL, no threshold, no strategy rule).** An ephemeral, never-persisted per-sweep generation token plus a per-pair status: every pair is marked `IN_PROGRESS` when the sweep starts, `FRESH` only after weekly/daily/fh/bucket/grade have ALL been written for that pair in that generation, `FAILED` on throw or on incomplete W/D/H4 data. `htfSnapshotOf` (unprotected) returns `null` unless the pair is `FRESH` in the CURRENT generation and all three HTF fields are present — and `htfAlignmentPasses` (protected, unchanged) already fails closed on `null` with `HTF_SNAPSHOT_MISSING`. Switching Auto Scan off invalidates every pair, because the state can no longer be refreshed. The map is memory-only, so **a page reload also fails closed**: a rehydrated `fxhub_scan` authorises nothing until one complete sweep runs. **The bucket is still not cleared** — the frozen-semantics boundary the v12.36.2 comment drew is preserved; the repair withholds the SNAPSHOT, not the display state. **ZERO protected functions or constants changed; drift 0.** `index.html` +86/−2 (the two removed lines are the `else if(autoScanTimer)` branch). 42 new fixtures in `tests/v1240_scan_freshness_tests.js` (G2.1–G2.10) including a positive control, a bucket-independence discriminator, a multi-pair isolation control and a source-order wiring check; **8/8 mutations killed** — two survived the first pass and both were real test gaps of mine (a recovery fixture that never exercised `markFresh` against a `FAILED` entry, and a multi-pair control that drove helpers directly and so could not see a mutation inside the production catch). Full lane 35 suites / **2671 assertions / 0 failures**, Python 1505 OK, both count guards in sync. Four pre-existing suites needed an explicit, labelled test-harness shim: they seed `scanData` directly and had always implicitly assumed the completed sweep that in production is the only writer of those fields. **NOT FULLY CLOSED:** this closes the CURRENT-SWEEP authorization path. It does not create a per-pair evaluation timestamp, so nothing can state the AGE of a snapshot — see B-8/KNOWN_ISSUES §"No per-pair evaluation timestamp". A completed manual "Run Auto Top-Down Scan Now" while Auto Scan is OFF yields a genuinely current sweep that then remains current with no time bound, because no JVM contract defines one. |
| G-4 | **REPAIRED (uncommitted, under review) — owner-authorized protected change, exactly one function.** `alexGCloseLivePosition` built its closed-position record with two fabricating fallbacks: `m.exitDetectionSource\|\|'live_snapshot'` asserted HOW an exit was detected and `m.exitDetectedAt\|\|Date.now()` invented WHEN, whenever a caller supplied neither. Both fields sit INSIDE the evidence package's `contentHash` (not in `EVIDENCE_HASH_EXCLUDED_FIELDS`), so an invented value re-derived cleanly and was indistinguishable from an observed one. The strongest evidence was the line's own neighbours: `exitTriggerLevel`, `exitCandleStart` and `exitCandleEnd` all use `!=null` and fail closed to null; these two alone used `\|\|` and fabricated. **REACHABILITY, measured:** four call sites exist. Lines 5628/5644/5646 (`alexGCheckLivePositions`, unprotected) supply both fields; line 6101 (`generateTestAlexTrade`, gated on `developerModeEnabled`, reachable only from four Developer Mode buttons) supplies `exitDetectionSource:'developer_test'` but **NO `exitDetectedAt`**. So on the only reachable path **the timestamp alone was actively fabricated**; the source was not, because all four call sites already supply one — which makes the removed `\|\|'live_snapshot'` fallback **latent defence-in-depth against a future caller omitting provenance**, not the repair of a live fabrication. That path's `AGT\|TEST\|` packages are refused by import policy and produce zero observations. **Blast radius, measured:** `import_mogo_observations` maps `closedAt<-exitTimestamp`, `marketExitAt<-exitCandleEnd`, `exitPrice` and `exitDetectionSource`, and maps NOTHING from `exitDetectedAt` — so the fabricated timestamp never reached the 263-observation corpus, any forward statistic, the graph, or LEAN (zero occurrences in the Mode A package or any vector file). This was a provenance defect sealed into preserved evidence, not a trading or parity defect, and the diagnostic said so rather than inflating it. **COVERAGE CORRECTION (post-commit audit).** The first fixture set drove `alexGCloseLivePosition` directly and fabricated an `AGT\|TEST\|` id by hand — that proves the normalization but NOT the call site, and a report of mine over-claimed it exercised the developer path. `runV1241DeveloperPathFixtures` now invokes the real `generateTestAlexTrade` behind a proven `developerModeEnabled` gate and asserts the production-PRODUCED id form, `developer_test` preserved, `exitDetectedAt===null`, that the value is not near the runtime clock, and the same through the real evidence-package path. **REPAIR:** both fields now VALIDATE rather than null-check. `exitDetectedAt` requires a finite positive number (every legitimate caller supplies epoch ms — `exit.candleEnd` = `c.t+candleDurationMs`, or `Date.now()`); `exitDetectionSource` requires a non-empty trimmed string (the three legitimate values are `historical_candle`, `live_snapshot`, `developer_test`). `NaN`, `±Infinity`, `0`, negatives, booleans, objects, arrays and whitespace-only strings are UNKNOWN, serialized as JSON `null` — the representation `pick()` and the package schema already use. No numeric-string coercion: no production caller supplies one. **A close is never REJECTED for unknown optional provenance** — Option B was rejected as trading a provenance defect for an availability defect. 69 fixtures in `tests/v1241_exit_provenance_tests.js` driving the real production function (never a reimplementation), with six positive controls and 9/9 mutations killed including `\|\|Date.now()` restoration, `\|\|'live_snapshot'` restoration, `!=null`-only checking, UNKNOWN→0, UNKNOWN→invented label, `typeof`-only checks that would accept `NaN`/empty strings, and a coercion mutation. **Two pre-existing fixtures asserted the invented default itself** (`ALEX-CLOSE-SOURCE.1` said "defaults to ... live_snapshot"); they were about JOURNALLING a source verbatim, not about defaulting one, so the source is now supplied explicitly and both keep their original exact-literal strength — not weakened. Full lane 36 suites / **2740 assertions / 0 failures**, Python 1505 OK, both count guards in sync. **Protected baseline deliberately updated: exactly one function (`alexGCloseLivePosition`) rebaselined, 64/64 functions and 4/4 constants accounted for, no other entry changed, `knownGoodHtmlSha1` unchanged.** **DEFERRED, not fixed:** line 5628 passes `exit.candleEnd` — a CANDLE BOUNDARY — into a field named "detected at", conflating market time with detection time on every historical re-walk close. Splitting `exitOccurredAt`/`exitDetectedAt`/`exitProcessedAt` is Option C and is explicitly out of scope here. |
| B-32.25 | **RESOLVED — the G-2 mutation evidence is now reproducible from a fresh checkout, and its strength is no longer uniform-by-assertion.** `7339bec` reported "8/8 mutations killed" for the G-2 scan-eligibility repair. The repair was real and behaviourally tested (42 assertions, still 42/42), but the mutations were run from an ad-hoc shell function and **never committed** — so the claim was unverifiable by anyone who had not run it, and only 3/8 were ever re-run as spot checks. `tests/mutate_v1240_scan_freshness.js` is that evidence, committed, plus a `tests/run_all.sh` stage. **G-2 PRODUCTION BEHAVIOUR IS UNCHANGED** — `index.html` is byte-identical and this is test infrastructure only. **THE HISTORICAL 8/8 IS REPRODUCED, AND FIVE OF THOSE KILLS ARE BEHAVIOURAL WHILE THREE ARE STRUCTURAL-CONTRACT.** H1 (remove the sweep-start generation bump), H2 (mark FRESH before the complete refresh finishes) and H7 (one pair's catch invalidates every pair) all mutate call sites inside `runAutoTopDownScan`, which is `async` and unreachable by the JXA behavioural harness — the same permanent limitation recorded for `runDiagnostics` (v12.1.1) and `simulateTrueMTFReplay` (v12.1.2). They are killed by v1240's G2.10 source-order assertions: a genuine kill, a named assertion failing for the intended reason, but **weaker evidence than an end-to-end behavioural kill, and this is not full end-to-end mutation coverage**. H3/H4/H5/H6/H8 are behavioural. **TWO ADDITIONAL MUTATIONS were added and are labelled as such, never folded into the historical claim:** A1 (generation never advances) and **A2 (Auto Scan OFF no longer invalidates)** — both behavioural. A2 matters most: **the original eight never covered Auto Scan OFF**, which is the exact toggle combination that exposed G-2 in the first place. Combined: **10/10 killed — 7 behavioural, 3 structural-contract.** The historical M2 naming ambiguity is resolved: the mutation actually run at `7339bec` was mark-FRESH-before-completion (now H2); "reuse the same generation" was never in the historical set and is filed as A1. **NON-DESTRUCTIVE:** each mutation goes to a COPY of `index.html` in a fresh temp dir with the suite redirected by cwd, temp dirs removed in `finally`, and the repository file's SHA-256 re-checked after every mutation — a mismatch aborts. `git restore` is never used as cleanup. **AN UNMUTATED CONTROL RUNS FIRST** and must reproduce 42/42 in the temp tree; without it a "SURVIVED" could equally mean the suite had silently loaded the real `index.html`. **The control immediately earned its place:** the first implementation read only stdout, but `osascript` writes JXA `console.log` to **stderr**, so every transcript was empty and every mutation looked like a loader failure. **SELF-DISCRIMINATION, 7/7:** missing anchor → NOT APPLIED; ambiguous anchor → AMBIGUOUS (2858 matches); syntax-breaking → SYNTAX INVALID and explicitly not a kill; no-op → SURVIVED; missing script body → loader error, not a kill; wrong discriminator name → KILLED FOR THE WRONG REASON; hash mismatch detected. All exercised on temporary synthetic inputs; no repository file was modified to test a failure path. **A DEDICATED GATE, NOT A REGISTERED SUITE** — its assertions are about whether v1240 can still detect a broken G-2, so counting them would conflate "the code is correct" with "the tests can notice incorrectness". `expected_fixture_counts.tsv` stays at 2754, v1240 stays registered once at 42. The gate re-runs v1240 eleven times against temp copies (~2 s); that duplicated runtime is disclosed in the stage comment rather than hidden. Run directly with `node tests/mutate_v1240_scan_freshness.js`, or `--self-test`. |
| B-32.27 | **RESOLVED — the platform-health mutation evidence is now reproducible from a fresh checkout, and the previously reported mechanism is retracted.** `994e2da`'s deterministic-fixture repair **remains correct and is unchanged**: the selftest's live-store GREEN case had passed `REPO_ROOT` with no injected clock and decayed into a failure once the repository had not been written for `STORE_STALE_AFTER_HOURS`; `check_live_store` was right throughout and is byte-identical across `e282d99`, `994e2da` and HEAD. **What is retracted is that commit message's mutation evidence, not its repair.** It reported "7/7 killed" from a scratch tool that was never committed, and described the mutant as "re-pointed at via `sys.path`". That mechanism was re-examined and does not hold. **(a) Naive `sys.path.insert(0, tmpdir)` does not redirect the import at all** — `test_platform_health` performs its own `sys.path.insert(0, REPO_ROOT/scripts/trader_intelligence)` when imported, which wins, so the REAL module loads and every mutation would survive silently. **(b) Pre-seeding `sys.modules` does load the mutant but leaves the TEST module rooted in the real repository**, so its `REPO_ROOT` points at a tree the mutant knows nothing about, `test_the_real_corpus_classifies_and_attributes_cleanly` loads an empty corpus and reports RED, and that unrelated failure was the ONLY failure produced. **A no-op "mutation" was measured scoring as a kill under those conditions**, which makes any 7/7 obtained that way meaningless. A nonzero unittest exit is therefore not evidence of a kill, and neither is "some test failed"; the original mutation evidence is **not** claimed to have been valid. **CORRECTED DESIGN — MIRRORED TREE.** `tests/trader_intelligence/mutate_platform_health.py` copies the module tree's `.py` files AND the test module under ONE temporary root, so the test module's own `REPO_ROOT` resolves inside that root and the structural source-reading assertion reads the **mutated** file. Provenance is proven per run by a realpath-normalised probe (macOS returns `/var/…` from `tempfile` and `/private/var/…` from the child; the naive string compare reports a false provenance failure) asserting `platform_health.__file__` is under the temp root, `REPO_ROOT` **is** the temp root, the structural assertion's target path and sha256 are the mutated file's, and that **no module was imported from the real repository**. `sys.path` insertion is never accepted as proof of redirection. **FOCUSED BY CONSTRUCTION:** only `TestLiveStoreFreshnessIsDeterministic` runs — exactly **10** collected tests, verified before being hard-coded — because only those ten discriminate P1–P7. **The observation corpus is never copied, symlinked, opened or depended on**, and `docs/` is not linked into the tree, so no corpus behaviour can influence a verdict; that dependency is the precise defect that invalidated the earlier evidence. The full 31-test module still runs once per lane against the real committed source. **RESULT: 7/7 killed — 6 BEHAVIOURAL, 1 STRUCTURAL-CONTRACT.** P2 (ignore the injected clock), P3 (fresh→YELLOW), P4 (stale→GREEN), P5 (`>`→`>=`), P6 (bypass the threshold at the resolution site, a different source line from P4) and P7 (missing→GREEN) are behavioural. **P1 (reintroduce `REPO_ROOT` as the GREEN selftest fixture) is STRUCTURAL-CONTRACT and is reported as such** — killed by `test_the_selftest_live_store_fixtures_are_controlled_not_the_repository`. No behavioural assertion can distinguish a `REPO_ROOT` fixture from a controlled one without depending on the repository's current mtime, which is the very time-dependence being designed out; a "behavioural" kill of P1 would fire or not according to the calendar. Measured during the diagnostic: the repository root was 22.8 h stale against a 12 h threshold, so such a kill would have fired **by luck** that day. **This is not uniform behavioural coverage and must not be described as such.** **NON-DESTRUCTIVE:** each mutation is applied independently to a FRESH temporary copy, never in place; `PYTHONDONTWRITEBYTECODE=1` and `__pycache__`/`.pyc` are excluded from the copy; temp trees are removed in `finally`; the sha256 of `platform_health.py`, `test_platform_health.py`, `index.html`, `regression-baseline.json` and `expected_python_test_counts.tsv` is re-checked and drift aborts nonzero. `git restore` is never used as cleanup. **AN UNMUTATED CONTROL RUNS FIRST** and must collect exactly 10 with zero failures and correct provenance; without it "SURVIVED" and "the suite never really ran" are indistinguishable — and a no-op scoring as a kill is exactly how the earlier evidence went wrong. **SELF-DISCRIMINATION, 9/9 (`--self-test`):** missing anchor → NOT APPLIED; ambiguous anchor → AMBIGUOUS (19 matches); syntax-breaking → SYNTAX INVALID, explicitly not a kill; no-op → NO-OP; module-level import break → LOADER/SETUP ERROR; wrong collected count → LOADER/SETUP ERROR; wrong discriminator name → KILLED FOR WRONG REASON; provenance failure (the probe pointed at the real repository, the exact condition the naive approach produced) → PROVENANCE FAILURE, listing the six modules that leaked; hash mismatch → DRIFT DETECTED. All exercised on temporary synthetic inputs; **no repository file was modified to test a failure path**, and the hash case corrupts the expected value rather than the file. A probe that cannot RUN is classified LOADER, not PROVENANCE, so the reported cause is the real one. **A DEDICATED GATE, NOT A REGISTERED SUITE** — the filename does not start with `test_`, the prefix both `run_all.sh` and `count_python_tests.py` enumerate by, so **1515 tests / 30 modules are unchanged and no count file was touched**. The gate re-runs the focused class eight times against temp trees (~3 s); that duplicated runtime is disclosed in the stage comment rather than hidden. Run directly with `python3 tests/trader_intelligence/mutate_platform_health.py`, or `--self-test`. **TEST INFRASTRUCTURE ONLY — no production file changed; `994e2da` is not amended.** |
| B-32.28 | **RESOLVED — the G-4 mutation evidence is now reproducible from a fresh checkout, and it is uniformly BEHAVIOURAL.** `e282d99` reported "11/11 mutations killed" for the exit-provenance repair. The repair was real and behaviourally tested (83 assertions, still 83/83), but the mutations came from a scratch tool that was **never committed**, so the claim was unverifiable by anyone who had not run it. This was the last of the three instances of that debt — G-2 closed at `f68f2d2`, platform-health at `ca78fab`, G-4 here. `tests/mutate_v1241_exit_provenance.js` is that evidence, committed, plus a `tests/run_all.sh` stage. **G-4 PRODUCTION BEHAVIOUR IS UNCHANGED** — `index.html` is byte-identical, its sha256 is re-verified after every mutation, and `alexGCloseLivePosition` is a PROTECTED function, so a harness that edited it in place would show as protected drift. **THE HISTORICAL 11/11 IS REPRODUCED, AND UNLIKE G-2 EVERY KILL IS BEHAVIOURAL — 11 behavioural, 0 structural-contract.** M1/M2 restore the two fabricating fallbacks (`\|\|Date.now()`, `\|\|'live_snapshot'`); M3/M4 weaken both checks to `!= null` only; M5 serializes an UNKNOWN timestamp as `0` and M6 an UNKNOWN source as an invented label; M7/M8 are `typeof`-only checks that accept `NaN` and the empty string respectively; M9 coerces a numeric string into a real timestamp. **M10 and M11 mutate the DEVELOPER CALL SITE in `generateTestAlexTrade`** — adding `exitDetectedAt:Date.now()`, and removing `exitDetectionSource` — and only the real-path DEV fixtures can catch them: driving `alexGCloseLivePosition` directly would never see a caller supplying the wrong thing, which is the coverage correction `e282d99` itself recorded. Each mutation names ONE discriminator that must fail for the intended reason; several have wide reach (M1 fails 31 assertions, M9 exactly 1) but reach is never accepted as a kill. **NON-DESTRUCTIVE:** every mutation goes to a COPY of `index.html` in a fresh temp dir with the suite redirected by cwd, temp dirs removed in `finally`, and `git restore` is never used as cleanup. **AN UNMUTATED CONTROL RUNS FIRST** and must reproduce 83/83 in the temp tree. **SELF-DISCRIMINATION, 8/8 (`--self-test`):** missing anchor → NOT APPLIED; ambiguous anchor → AMBIGUOUS (2858 matches); syntax-breaking → SYNTAX INVALID, explicitly not a kill; no-op → NO-OP; wrong discriminator → KILLED FOR THE WRONG REASON; wrong collected count → COLLECTION CHANGED; a valid script that throws on load → LOADER ERROR; hash mismatch detected. All on temporary synthetic inputs; no repository file was modified to test a failure path. Both streams are concatenated when reading the suite, because `osascript` writes JXA `console.log` to **stderr** — reading stdout alone yields an empty transcript indistinguishable from a loader failure, the defect the G-2 harness's control caught, recorded here so it is not rediscovered. **A DEDICATED GATE, NOT A REGISTERED SUITE** — the filename does not match `tests/run_*_tests.js`, so `expected_fixture_counts.tsv` stays at 2754 and v1241 stays registered once at 83. The gate re-runs v1241 twelve times against temp copies (~20 s); that duplicated runtime is disclosed in the stage comment. **STILL DEFERRED, unchanged by this commit:** the historical re-walk call site passes `exit.candleEnd` — a CANDLE BOUNDARY — into a field named "detected at", conflating market time with detection time. Splitting `exitOccurredAt`/`exitDetectedAt`/`exitProcessedAt` remains out of scope. Run directly with `node tests/mutate_v1241_exit_provenance.js`, or `--self-test`. |
| G-3 (triage) | **PRIORITISED, NOT REPAIRED — diagnostics-only, verified in code; the repair is a narrow product decision and is NOT blocking LEAN work.** The contradiction is real and reproducible from source: `fetchCandlesRange` was repaired in v12.20.0 so a short page is **not** proof of exhaustion (it continues the walk; short pages are counted as a `shortPages` diagnostic, and only `EMPTY_PAGE`, `HTTP_ERROR`, `NETWORK_ERROR`, `IDENTITY_MISMATCH`, `PAGE_INTEGRITY` or `CURSOR_NOT_ADVANCING` terminate it), while `runHistoricalDataDiagnostic` still ends its walk on `if(rawCount<count) break;`. **BLAST RADIUS, MEASURED: nil for trading.** `runHistoricalDataDiagnostic` has exactly ONE caller — the Diagnostics-tab button handler, which passes the result to `renderDataDiagnostic()` and a status string. It reaches no scanner decision, no bias, no paper trade, no evidence package and no LEAN input. Neither function is protected. The production consumer of `fetchCandlesRange` is `fetchAlexGReplayDatasets` (replay/backtest), which already has the corrected semantics. **FAILURE DIRECTION: under-reports.** A truncated walk claims LESS available history than exists; it cannot authorise anything and cannot cause a trade. **WHY IT STILL MATTERS:** the tool's own UI text states it is "the concrete proof behind the `fetchCandlesRange()` pagination fix — run it yourself against your real account to see the verified numbers rather than take it on faith." A diagnostic that truncates therefore under-reports the very fix it exists to demonstrate, and could lead an operator to conclude pagination is still broken — the exact "~48-50 day history ceiling" symptom v2.9 and v12.20.0 each removed. **WHY IT WAS NOT REPAIRED HERE — the exact decision needed.** `MDHIST-2` does not merely tolerate the truncation, it asserts it in prose: *"one request only — a short page means the history is exhausted"* and *"reports the 4 bars it genuinely has rather than walking on"*, and its comment says it exists to pin BOTH sides of the termination boundary so neither `<` nor `<=` can be substituted. Repairing the production line requires INVERTING a deliberately-written committed fixture that states the opposite contract. Two defensible readings exist — (a) the diagnostic must mirror the paginator it exists to prove, or (b) it is an independent probe whose simpler termination is intentional — and the operator's ruling decides which. **Recommendation: (a)**, on the strength of the UI text, with `MDHIST-2` rewritten to assert continuation and a positive control that a genuinely empty page still terminates. Not guessed at here. |
| LEAN Mode B | **BLOCKED FROM ENGINE EXECUTION — two independent, structural blockers; local verification is complete and passes.** **(1) No credentials and no local runtime.** `LEAN_RUN_RECORD.md` records that the QuantConnect account was created by the **operator personally** and that "no agent created it or holds credentials for it"; the `lean` CLI is not installed and Docker is not installed, so no local LEAN engine exists either. **(2) The adopted transport requires publishing licensed market data.** After Object Store write/read and project `LocalFile` all failed in cloud probes, the variant adopted `RemoteFile`, whose own README states the corpus must be published as **one public gist** — "putting 1,048 preserved OANDA candles on a public host… world-readable, indexable, archivable, effectively irreversible". That is prohibited three ways by the standing authorization: no public gists or artifact publishing, no uploading licensed OANDA data, and no sharing links. `url_map.py` currently points at `https://REPLACE-ME.invalid/`, so nothing is published. **LOCAL STATUS: PASS, and honestly labelled.** Both variants reproduce: the Object Store variant's suite and the RemoteFile variant's 116 checks both pass, including a two-sided future-poison control (poisoning bars strictly AFTER the lock changes nothing 15/15; poisoning FROM the lock bar onward DOES move the decision 15/15 — so the control is not vacuous) and an independence control (decisions and full per-bar ledgers identical with the `expected` block emptied, 15/15). Algorithm hashes recorded, `br_machine.py` byte-identical across variants at `29e29578…`. **The package's own test output says it plainly: "This says NOTHING about whether it works in QuantConnect."** A local harness run is NOT a LEAN engine run and is not reported as one. **NOTE ON MODE A:** its `Creative Red Panda` run WAS hosted by LEAN (`engine=LEAN`, algorithm `654fcaf5…`, v2.5.0.0.18029), but the record states the algorithm "adds no securities and subscribes to no data feed, and the decision loop reads only the embedded bars" — 14 data points. Mode A therefore does **not** establish that candles traverse LEAN's event-driven data path; that is precisely what Mode B exists to establish, and it remains unestablished. |
| Mode A break-cycle | **CAUSE UNDETERMINED AND NOT SETTLEABLE FROM THIS CORPUS — deliberately not fitted.** 10/15, with the five divergent cases fully characterised: case 08 window [184..239] recorded 209 / derived 225, crossings [209, 217, 225]; case 09 [677..779] recorded 700 / derived 720, crossings [677, 687, 700, 720]; case 10 [178..244] recorded 217 / derived 219, crossings [199, 217, 219]; case 11 [611..790] recorded 633 / derived 652, crossings [633, 652]; case 12 [1538..1595] recorded 1547 / derived 1572, crossings [1547, 1564, 1567, 1572]. The implementation selects the most recent break before the anchor; **the recorded answer is not the first crossing either** (case 09 selects the third of four, case 10 the second of three), so no trivial substitution resolves it. An earlier attribution to `INSUFFICIENT_PREWINDOW_STATE` was **retracted and refuted by the data** — in all five cases the recorded break bar lies INSIDE the preserved window. Leading unproven candidate: the supplied zone bounds are geometry *as at qualification*, and MOGO's zone may have held different bounds when the earlier crossings occurred, making those crossings anachronistically scored; the preserved evidence does not carry the zone's earlier bounds. **SCOPE: the divergence does not propagate.** Entry, stop and target derive from the retest bar and the zone edges, never from the break bar — qualification, direction, retest, entry, stop, target and ATR are all 15/15. **NO SELECTION RULE HAS BEEN FITTED TO THE RECORDED ANSWERS, DELIBERATELY**, in this sprint or the last: doing so would manufacture agreement rather than demonstrate it. Resolving it requires new evidence — per-bar zone geometry over the window — not more analysis of this corpus. |
| Break-cycle evidence spec | **SPECIFICATION ONLY — no production instrumentation implemented, and the missing-geometry hypothesis is NOT asserted as the cause.** The five divergences (cases 08-12) remain unresolved and no selection rule has been fitted. What follows is the MINIMUM capture that would let the cause be tested rather than guessed; it is a proposal for operator authorization, not a change. **WHAT IS MISSING.** The preserved corpus carries zone bounds as at QUALIFICATION only. Every earlier crossing is therefore scored against geometry that may not have been the geometry in force when it occurred. That is a *candidate* explanation, explicitly unproven — it predicts the divergence but has never been tested, because the data needed to test it does not exist. **MINIMUM CAPTURE, per evaluated zone per bar, from the bar the zone becomes available to the bar the decision locks:** (1) `barIndex` and the bar's own close time; (2) `zoneLow` and `zoneHigh` **as held at that bar**, not as at qualification — this is the single field whose absence blocks the analysis; (3) `zoneRole` at that bar, since a role flip would re-point the break direction; (4) `zoneRevisionId` — a counter incremented whenever either bound or the role changes, so a re-anchor is visible as an event rather than inferred by diffing floats; (5) whether the bar was scored as a confirmed break, and against which bound; (6) the ATR value in force at that bar, because the displacement test scales with it. **WHAT WOULD SETTLE IT.** With (2) and (4) captured, each of the five cases can be re-scored against the bounds actually in force at each crossing. If the recorded selection then falls out of the documented rule, the hypothesis is confirmed and the adapter needs no rule change — only correct inputs. If it does not, the hypothesis is refuted and the difference is a genuine rule divergence to be specified. **Either outcome is a result; neither requires inventing a selection rule.** **CONSTRAINTS.** Capture is diagnostic-only and must not alter zone formation, break detection, retest scoring or any threshold; it is per-evaluated-zone and bounded by the existing staleness window, so it cannot grow without limit; and it produces new forward evidence only — it cannot reconstruct the history of the 15 preserved cases, which is why this is a forward-capture specification and not a re-analysis plan. **The five cases stay unresolved until such evidence exists.** |
| Reporting corrections | **DURABLE CORRECTION of two inaccurate claims made in this work, recorded here because the commit messages that carried them are already written and are not being rewritten.** **(1) "20 FIXTURES ADDED" in `76b4f24` is wrong.** The accurate figures are **13 authored test methods and 15 newly collected tests** — `TestCheckModeValidatesWithoutWriting` contributes 8 authored methods and re-collects 2 INHERITED from `TestTheCliActuallyUsesTheExitCode` (it subclasses it), and `TestRunAllUsesCheckModeAndPropagatesFailure` contributes 5. 8 + 5 = 13 authored; + 2 inherited = 15 collected, which reconciles `test_evidence` 296 → 311 and the Python total 1535 → 1550 exactly. "20" was a reporting error — not tests, not assertions, not subtests. No test was removed, renamed, skipped or left undiscovered, and no baseline was adjusted to match output. **(2) "6/6 mutations killed, all behavioural" for the `--check` work is wrong.** The correct split is **4 behavioural and 2 structural-contract**. Behavioural: C1 (`--check` still writes → `test_check_mode_does_NOT_create_the_reports_directory`), C2 (`--check` always returns 0 → `test_check_mode_FAILS_an_invalid_corpus…`), C3 (`--check` reads the cache → `test_check_mode_RECOMPUTES_and_ignores_a_stale_report…`), C4 (no-arg stops writing → `test_POSITIVE_CONTROL_the_no_arg_run_DOES_write_the_report`) — each executes the CLI and observes exit code or filesystem effect. **Structural-contract:** C5 (run_all drops `--check` → `test_both_validator_stages_invoke_check_mode`) and C6 (run_all stops propagating → `test_the_guard_wraps_both_validator_stages`) — both are satisfied by reading `run_all.sh` source text, with no execution. `test_a_failing_validator_stage_FAILS_the_run` IS behavioural but did not kill C6: it executes a synthetic fragment, so it proves the guard SHAPE works while the structural test proves `run_all.sh` actually uses that shape. A source-wiring assertion is structural-contract unless an executed behavioural discriminator establishes otherwise. **(3) Also retracted, and recorded at the time: the claim that no raw OANDA-derived artifact was opened.** Two Mode B harnesses executed in an earlier sprint DID read the 15 corpus CSVs and the embedded historical vectors through their own imports. Nothing was committed, copied into the repository or published, and no candle value entered the transcript — but the reads happened, and the isolation assertions in `test_lean_synthetic_smoke.py` are the durable form of that lesson. |
| LEAN Mode B (synthetic) | **ESTABLISHED — LEAN's event-driven data path is proven, on synthetic inputs only.** Observed run: project `35863117`, backtest `Calm Yellow Pig`, algorithm `3fca3b003a7f8db84f343512ea9835fa`, LEAN `v2.5.0.0.18041`, 2026-08-30. **22/22 SMOKE-CHECK PASS; `SMOKE-VERDICT PASS engine=LEAN failed=none`.** SYNQUAL: 120 bars, LOCKED, decision=True, locked_at=55. SYNREJ: 120 bars, BROKEN, decision=False, locked_at=None. The log is preserved byte-for-byte at `tests/lean_synthetic/evidence/CALM_YELLOW_PIG_2026-08-30.log` (sha256 `83e377e54c5dc93b83416fe023598425d2e431a4c1767a63b329d149c71b1ecd`) with a full run record beside it, and 21 independent assertions were run over the log bytes before it was accepted — one launch record and one completion record naming the same and only algorithm id, 22 checks split 10/10/2 with no duplicate name and the string `FAIL` absent, both cases running the identical check set, every logged check name declared in the committed algorithm, and outcomes equal to its `EXPECT` literals. **WHAT IS NOW ESTABLISHED:** LEAN's own subscription pipeline delivered 120 synthetic bars per case into `on_data`; the reviewed state machine (unchanged, sha256 `29e29578…`) consumed each exactly once, in order, with no bar arriving before its own `EndTime`; a constructed qualifying setup produced a decision and a constructed invalid setup produced none; per-case state stayed isolated and no unexpected symbol was delivered. This is the FIRST run in the project to establish event-driven delivery at all — Mode A's `Creative Red Panda` ran inside LEAN but subscribed to no feed and read embedded bars (14 data points). It took three attempts: symbol routing (`KeyError: QC-UNIVERSE-USERDEFINED-USA-BASE`), then field types (`set_item: (str, int)`), then this. **WHAT IS NOT ESTABLISHED, and must not be read into it: HISTORICAL MODE B PARITY IS NOT ESTABLISHED.** Nothing here speaks to the 15 preserved cases; the synthetic corpus is 2 constructed cases of 120 bars generated from documented arithmetic, with no market observation of any kind. **THE FIVE MODE A BREAK-CYCLE DIVERGENCES REMAIN UNRESOLVED** — cause still UNDETERMINED and still not settleable from the preserved corpus, and no selection rule has been fitted. **NO ORDER EXECUTION, PROFITABILITY OR PRODUCTION-READINESS CLAIM** is supported: the run placed no orders, added no broker, held nothing and ended flat. **PROVENANCE CAVEAT, recorded rather than glossed:** the source that executed in the cloud was NOT downloaded from QuantConnect and NOT hash-compared against the repository. Its identity rests on the operator's reported `pbcopy` procedure from adapter commit `b1d46d3f89429464e6a647b465fd47075e765ebe` plus behavioural corroboration in the log (the two GLOBAL checks exist only in the repaired adapter; the preceding revision failed on every row where this one parsed all 240). Strong circumstantial agreement, not a hash. The two published fixtures WERE downloaded and hash-verified before use. LEAN's `983 data points` is a platform counter that was not derived here; it does not contradict 240 delivered bars, and the per-case counts of 120 are the algorithm's own. |
| Closeout corrections | **DURABLE CORRECTIONS to four claims made during the sandbox/test-separation work, recorded here because the commits carrying them are written and are NOT being rewritten.** **(1) "Seven moved tests plus two new preconditions = 9" (commit `11848df`) is imprecise.** Verified from source: **FIVE** tests were truly moved (originals removed) — `test_the_real_corpus_converts_without_loss`, `test_the_real_corpus_mints_nothing_new`, `test_2_every_indexed_artifact_exists_and_matches_its_whole_file_hash`, `test_3_package_hashes_verify_through_the_production_canonicalizer`, `test_no_LIVE_record_states_a_value_its_package_leaves_null`. **TWO were COPIED, not moved** — `test_package_ids_really_do_collide_across_capture_runs` and `test_content_hash_is_unique_across_every_package` still exist in `tests/trader_intelligence/test_import_mogo_observations.py`, retargeted at a synthetic glob, with the real-corpus version duplicated into the lane. Plus **two** new preconditions = 9. **(2) "Moved verbatim, not weakened" is wrong for one of the five.** `test_no_LIVE_record_states_a_value_its_package_leaves_null` had `self.skipTest("live corpus not present")` replaced by a hard `assertTrue`. It was **strengthened**, deliberately — in an opt-in lane an absent corpus is the failure, not a reason to go green — but it is not verbatim. The lane's own docstring said "moved here verbatim" and is corrected in this commit. **(3) Count arithmetic, re-derived and confirmed exact:** 1599 → 1597 is import 119→120 (+1: −2 moved out, +3 acceptance controls), backup 38→36 (−2), evidence 311→310 (−1), net −2. 1597 → 1666 is +22 `test_lean_preflight` and +47 `test_lean_import`. Modules 31 → 33. JS 2766 → 2769 (v131 73→76). No total was forced. **(4) SCOPE CORRECTION on "no raw-evidence access".** The opt-in run that produced nine failures was `MOGO_RUN_REAL_EVIDENCE=1 python3 -m unittest tests.integration_real_evidence.test_real_corpus_integration`, and its targets were the **ACTUAL restricted paths** (`REPO_ROOT/evidence/`), not synthetic stand-ins. The accurate statement is: **access to real evidence was ATTEMPTED and DENIED by the sandbox; zero bytes were successfully read.** "No raw-evidence access" is true of successful reads, not of attempts, and that distinction is the whole point of the OS denial. |
| Mutation evidence provenance | **SESSION-ONLY, NOT REPRODUCIBLE — recorded so the claim is not mistaken for committed evidence.** `git ls-files` shows exactly three committed mutation harnesses: `tests/mutate_v1240_scan_freshness.js` (G-2, 10/10), `tests/mutate_v1241_exit_provenance.js` (G-4, 11/11) and `tests/trader_intelligence/mutate_platform_health.py` (7/7). Those three are reproducible from a fresh checkout with `node`/`python3` plus `--self-test`. **Every other mutation figure reported during this work has NO committed harness** and was produced by scratch runners outside the repository: 6 preflight + 12 import (the "18/18"), 14 evidence-separation, 5 G-3 paginator, 5 EVF anchor-schema, 4 corpus-refusal, 3 B-18 bias, 7 adapter/interface. Those runs were real and their per-mutation results were reported honestly at the time, but **they cannot be re-run by anyone who did not have that session**, which is precisely the debt class closed three times already (B-32.25 for G-2, B-32.28 for G-4, and the platform-health harness). They are labelled **session-only evidence**. No new harness was built in the closeout; committing one for the preflight/import tools is the obvious next hardening step and is deliberately left unstarted. |
